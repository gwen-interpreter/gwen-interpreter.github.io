import Link from '@docusaurus/Link';

# DSL Steps

All Gherkin steps that match the DSL expressions here are readily executable by Gwen. The DSL matched is the operation that will be performed at execution time.

:::tip

Use DSL steps in your meta to compose StepDefs. Although it's possible to use them directly in feature specs too, features should not be DSL-aware. That's what meta is for!

:::

:::info We're migrating our docs

We are still updating this page and will post more here soon. In the meantime, see the complete DSL guide on our wiki by clicking the link below:

- [Supported DSL](https://github.com/gwen-interpreter/gwen-web/wiki/Supported-DSL)

:::

## Web pages

### Given

<details id="the-page-url-is-url">
  <summary class="dsl">

  ```gherkin
  the <page> url is "<url>"
  ```
  <p>Puts the given page into scope and binds the given URL to it.</p>
  </summary>
  <p>Where</p>
  <ul>
    <li><code>&lt;page&gt;</code> is the name of the page</li>
    <li><code>&lt;url&gt;</code> is the page URL (can be in <Link to="https://github.com/gwen-interpreter/gwen/wiki/Doc-Strings">DocString</Link> position)</li>
  </ul>
  <p>Example</p>

  ```gherkin {1}
    Given the todo page url is "https://todomvc.com/examples/react"
      And the todo field can be located by class "new-todo"
  ```

  <div class="grid-3">
    <div><Link to="#the-page-url-is-url">Link</Link></div>
    <div align="center"></div>
    <div align="right">Since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.41.0">v2.41.0</Link></div>
  </div>
</details>

<details id="i-am-on-the-page">
  <summary class="dsl">

  ```gherkin
  I am on the <page>
  ```
  <p>Puts the given page into scope and makes its bindings avaialble.</p>
  </summary>
  <p>Where</p>
  <ul>
    <li><code>&lt;page&gt;</code> is a page name</li>
  </ul>
  <p>Example</p>

  ```gherkin {2}
    Given I navigate to "https://todomvc.com/examples/react"
      And I am on the todo page
      And the todo field can be located by class "new-todo"
     When I enter "Walk the dog" in the todo field
  ```

  <div class="grid-3">
    <div><Link to="#i-am-on-the-page">Link</Link></div>
    <div align="center"></div>
    <div align="right">Since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v1.0.0">v1.0.0</Link></div>
  </div>
</details>

### When

<details id="i-navigate-to-the-page">
  <summary class="dsl">

  ```gherkin
  I navigate to the <page>
  ```

  <p>Navigates the browser to the URL bound to the given page and puts the page into scope. Starts a new browser window if one is not open.</p>
  </summary>
  <p>Where</p>
  <ul>
    <li><code>&lt;page&gt;</code> is the name of the page to navigate to</li>
  </ul>
  <p>Example</p>

  ```gherkin {2}
    Given the todo page url is "https://todomvc.com/examples/react"
     When I navigate to the todo page
  ```

  <div class="grid-3">
    <div><Link to="#i-navigate-to-the-page">Link</Link></div>
    <div align="center"></div>
    <div align="right">Since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v1.0.0">v1.0.0</Link></div>
  </div>
</details>

<details id="i-navigate-to-url">
  <summary class="dsl">

  ```gherkin
  I navigate to "<url>"
  ```

  <p>Navigates the browser to the given URL. Starts a new browser window if one is not open.</p>
  </summary>
  <p>Where</p>
  <ul>
    <li><code>&lt;url&gt;</code> is the URL to navigate to (can be in <Link to="https://github.com/gwen-interpreter/gwen/wiki/Doc-Strings">DocString</Link> position)</li>
  </ul>
  <p>Example</p>

  ```gherkin
    When I navigate to "https://todomvc.com/examples/react"
  ```

  <div class="grid-3">
    <div><Link to="#i-navigate-to-url">Link</Link></div>
    <div align="center"></div>
    <div align="right">Since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v1.0.0">v1.0.0</Link></div>
  </div>
</details>

<details id="i-capture-the-current-url">
  <summary class="dsl">

  ```gherkin
  I capture the current URL
  ```
  
  <p>Captures the current browser URL and binds it as <code>the current URL</code> in the global scope.</p>
  </summary>
  <p>Example</p>

  ```gherkin {1}
    When I capture the current URL
     And I navigate to "${the current URL}?q=gwen"
  ```

  <div class="grid-3">
    <div><Link to="#i-capture-the-current-url">Link</Link></div>
    <div align="center"></div>
    <div align="right">Since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v1.0.0">v1.0.0</Link></div>
  </div>
</details>

<details id="i-capture-the-current-url-as-name">
  <summary class="dsl">

  ```gherkin
  I capture the current URL as <name>
  ```
  
  <p>Captures the current browser URL and binds it to the given name in the global scope.</p>
  </summary>
  <p>Where</p>
  <ul>
    <li><code>&lt;name&gt;</code> is the name to bind the captured URL to</li>
  </ul>
  <p>Example</p>

  ```gherkin {1}
    When I capture the current URL as the base URL
     And I navigate to "${the base URL}?q=gwen"
    Then the current URL should start with the base URL
  ```

  <div class="grid-3">
    <div><Link to="#i-capture-the-current-url-as-name">Link</Link></div>
    <div align="center"></div>
    <div align="right">Since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v1.0.0">v1.0.0</Link></div>
  </div>
</details>

<details id="i-capture-the-text-in-the-current-url-by-regex-as-name">
  <summary class="dsl">

  ```gherkin
  I capture the text in the current URL by regex "<expression>" as <name>
  ```

  <p>Extracts a substring from the current browser URL using the given regex and binds it to the given name in the global scope.</p>
  </summary>
  <p>Where</p>
  <ul>
    <li><code>&lt;expression&gt;</code> is the regex expression</li>
    <li><code>&lt;name&gt;</code> is the name to bind the captured substring to</li>
  </ul>
  <p>Example</p>

  ```gherkin {1}
    When I capture the text in the current URL by regex "q=([^&]*)" as q
    Then q should be "gwen"
  ```

  <div class="grid-3">
    <div><Link to="#i-capture-the-text-in-the-current-url-by-regex-as-name">Link</Link></div>
    <div align="center"></div>
    <div align="right">Since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v1.0.0">v1.0.0</Link></div>
  </div>
</details>

<details id="i-refresh-the-current-page">
  <summary class="dsl">

  ```gherkin
  I refresh the current page
  ```

  <p>Refreshes the current page in the browser.</p>
  </summary>
  <p>Example</p>

  ```gherkin
    When I refresh the current page
  ```

  <div class="grid-3">
    <div><Link to="#i-refresh-the-current-page">Link</Link></div>
    <div align="center"></div>
    <div align="right">Since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v1.0.0">v1.0.0</Link></div>
  </div>
</details>

### Then

<details id="the-current-url-should-match-expression">
  <summary class="dsl">

  ```gherkin
  the current URL should[ not] <match> "<expression>"
  ```
  
  <p>Checks whether or not the current browser URL matches a given expression. Reports an assertion error if the match fails.</p>
  </summary>
  <p>Where</p>
  <ul>
    <li><code>not</code> negates the match if included</li>
    <li><code>&lt;match&gt;</code> is the type of match to perform, one of:
      <ul>
        <li><code>be</code> for exact match</li>
        <li><code>contain</code> for partial match</li>
        <li><code>start with</code> for partial leading match</li>
        <li><code>end with</code> for partial trailing match</li>
        <li><code>match regex</code> for regex match</li>
        <li><code>match xpath</code> for XML match</li>
        <li><code>match json path</code> for <Link to="https://github.com/json-path/JsonPath/blob/master/README.md">JSON match</Link>, since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v1.4.0">v1.4.0</Link></li>
        <li><code>match template</code> for <Link to="https://github.com/gwen-interpreter/gwen/wiki/Template-Matching">template</Link> match, since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.16.0">v2.16.0</Link></li>
        <li><code>match template file</code> for <Link to="https://github.com/gwen-interpreter/gwen/wiki/Template-Matching">template</Link> file match, since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.16.0">v2.16.0</Link></li>
      </ul>
    </li>
    <li><code>&lt;expression&gt;</code> is the expression to match (can be in <Link to="https://github.com/gwen-interpreter/gwen/wiki/Doc-Strings">DocString</Link> position)</li>
  </ul>
  <p>Example</p>

  ```gherkin {2,3}
    When I navigate to "https://todomvc.com/examples/react"
    Then the current URL should contain "todomvc.com"
     And the current URL should not contain "other.com"
  ```
  
  <div class="grid-3">
    <div><Link to="#the-current-url-should-match-expression">Link</Link></div>
    <div align="center"></div>
    <div align="right">Since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v1.0.0">v1.0.0</Link></div>
  </div>
</details>

<details id="the-current-url-should-match-expression-ref">
  <summary class="dsl">

  ```gherkin
  the current URL should[ not] <match> <expressionRef>
  ```
  
  <p>Checks whether or not the current URL in the browser matches a referenced expression. Reports an assertion error if the match fails.</p>
  </summary>
  <p>Where</p>
  <ul>
    <li><code>not</code> negates the match if included</li>
    <li><code>&lt;match&gt;</code> is the type of match to perform, one of:
      <ul>
        <li><code>be</code> for exact match</li>
        <li><code>contain</code> for partial match</li>
        <li><code>start with</code> for partial leading match</li>
        <li><code>end with</code> for partial trailing match</li>
        <li><code>match regex</code> for regex match</li>
        <li><code>match xpath</code> for XML match</li>
        <li><code>match json path</code> for <Link to="https://github.com/json-path/JsonPath/blob/master/README.md">JSON match</Link>, since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v1.4.0">v1.4.0</Link></li>
        <li><code>match template</code> for <Link to="https://github.com/gwen-interpreter/gwen/wiki/Template-Matching">template</Link> match, since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.16.0">v2.16.0</Link></li>
        <li><code>match template file</code> for <Link to="https://github.com/gwen-interpreter/gwen/wiki/Template-Matching">template</Link> file match, since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.16.0">v2.16.0</Link></li>
      </ul>
    </li>
    <li><code>&lt;expressionRef&gt;</code> is the name of the binding containing the expression to match
      <ul>
        <li>Can be the name of any binding that contains or resolves to an expression value, including a web element in which case the text in the element will be dynamically retrieved and used.</li>
      </ul>
    </li>
  </ul>
  <p>Example</p>

  ```gherkin {4,5}
    Given my domain is "todomvc.com"
      And some other domain is "someother.com"
     When I navigate to "https://todomvc.com/examples/react"
     Then the current URL should contain my domain
      And the current URL should not contain some other domain
  ```
  
  <div class="grid-3">
    <div><Link to="#the-current-url-should-match-expression-ref">Link</Link></div>
    <div align="center"></div>
    <div align="right">Since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v1.0.0">v1.0.0</Link></div>
  </div>
</details>

<details id="the-page-title-should-match-expression">
  <summary class="dsl">

  ```gherkin
  the page title should[ not] <match> "<expression>"
  ```
  <p>Checks whether or not the title of the current page in the browser matches a given expression. Reports an assertion error if the match fails.</p>
  </summary>
  <p>Where</p>
  <ul>
    <li><code>not</code> negates the match if included</li>
    <li><code>&lt;match&gt;</code> is the type of match to perform, one of:
      <ul>
        <li><code>be</code> for exact match</li>
        <li><code>contain</code> for partial match</li>
        <li><code>start with</code> for partial leading match</li>
        <li><code>end with</code> for partial trailing match</li>
        <li><code>match regex</code> for regex match</li>
        <li><code>match xpath</code> for XML match</li>
        <li><code>match json path</code> for <Link to="https://github.com/json-path/JsonPath/blob/master/README.md">JSON path</Link> match, since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v1.4.0">v1.4.0</Link></li>
        <li><code>match template</code> for <Link to="https://github.com/gwen-interpreter/gwen/wiki/Template-Matching">template</Link> match, since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.16.0">v2.16.0</Link></li>
        <li><code>match template file</code> for <Link to="https://github.com/gwen-interpreter/gwen/wiki/Template-Matching">template</Link> file match, since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.16.0">v2.16.0</Link></li>
      </ul>
    </li>
    <li><code>&lt;expression&gt;</code> is the expression to match (can be in <Link to="https://github.com/gwen-interpreter/gwen/wiki/Doc-Strings">DocString</Link> position)</li>
  </ul>
  <p>Example</p>

  ```gherkin {2}
    When I navigate to "https://todomvc.com/examples/react"
    Then the page title should contain "TodoMVC"
  ```

  <div class="grid-3">
    <div><Link to="#the-page-title-should-match-expression">Link</Link></div>
    <div align="center"></div>
    <div align="right">Since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v1.0.0">v1.0.0</Link></div>
  </div>
</details>

<details id="the-page-title-should-match-expression-ref">
  <summary class="dsl">

  ```gherkin
  the page title should[ not] <match> <expressionRef>
  ```
  <p>Checks whether or not the title of the current page in the browser matches a referenced expression. Reports an assertion error if the match fails.</p>
  </summary>
  <p>Where</p>
  <ul>
    <li><code>not</code> negates the match if included</li>
    <li><code>&lt;match&gt;</code> is the type of match to perform, one of:
      <ul>
        <li><code>be</code> for exact match</li>
        <li><code>contain</code> for partial match</li>
        <li><code>start with</code> for partial leading match</li>
        <li><code>end with</code> for partial trailing match</li>
        <li><code>match regex</code> for regex match</li>
        <li><code>match xpath</code> for XML match</li>
        <li><code>match json path</code> for <Link to="https://github.com/json-path/JsonPath/blob/master/README.md">JSON path</Link> match, since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v1.4.0">v1.4.0</Link></li>
        <li><code>match template</code> for <Link to="https://github.com/gwen-interpreter/gwen/wiki/Template-Matching">template</Link> match, since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.16.0">v2.16.0</Link></li>
        <li><code>match template file</code> for <Link to="https://github.com/gwen-interpreter/gwen/wiki/Template-Matching">template</Link> file match, since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.16.0">v2.16.0</Link></li>
      </ul>
    </li>
    <li><code>&lt;expressionRef&gt;</code> is the name of the binding containing the expression to match
      <ul>
        <li>Can be the name of any binding that contains or resolves to an expression value, including a web element in which case the text in the element will be dynamically retrieved and used.</li>
      </ul>
    </li>
  </ul>
  <p>Example</p>

  ```gherkin {3}
    Given the app name is "TodoMVC"
     When I navigate to "https://todomvc.com/examples/react"
     Then the page title should contain the app name
  ```

  <div class="grid-3">
    <div><Link to="#the-page-title-should-match-expression-ref">Link</Link></div>
    <div align="center"></div>
    <div align="right">Since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v1.0.0">v1.0.0</Link></div>
  </div>
</details>

## Web elements

### Given

<details id="element-can-be-located-by-selector-expression">
  <summary class="dsl">

  ```gherkin
  <element> can be located by <selector> "<expression>"
  ```

  <p>Binds a selector for a web element to the current scope with a default lookup timeout.</p>
  </summary>
  <p>Where</p>
  <ul>
    <li><code>&lt;element&gt;</code> is the web element name</li>
    <li><code>&lt;selector&gt;</code> is the selector type, one of:
      <ul>
        <li><code>id</code> for selecting by id attribute</li>
        <li><code>name</code> for selecting by name attribute</li>
        <li><code>tag name</code> (or <code>tag</code> since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.46.0">v2.46.0</Link>) for selecting by tag name</li>
        <li><code>css selector</code> (or <code>css</code> since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.46.0">v2.46.0</Link>) for selecting by css expression</li>
        <li><code>class name</code> (or <code>class</code> since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.46.0">v2.46.0</Link>) for selecting by class attribute</li>
        <li><code>xpath</code> for selecting by XPath</li>
        <li><code>link text</code> for selecting by link text</li>
        <li><code>partial link text</code> for selecting by partial link text</li>
        <li><code>javascript</code> (or <code>js</code> since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.46.0">v2.46.0</Link>) for selecting <Link to="https://gweninterpreter.wordpress.com/2019/08/13/locating-web-elements-with-gwen/#byJavaScript">by JavaScript</Link></li>
      </ul>
    </li>
    <li><code>&lt;expression&gt;</code> is the selector expression</li>
  </ul>
  <p>Example</p>

  ```gherkin {1}
    Given the todo field can be located by class "new-todo"
     When I enter "Walk the dog" in the todo field
  ```

  <div class="grid-3">
    <div><Link to="#element-can-be-located-by-selector-expression">Link</Link></div>
    <div align="center"><Link to="https://gweninterpreter.wordpress.com/2019/08/13/locating-web-elements-with-gwen/">Learn more</Link></div>
    <div align="right">Since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v1.0.0">v1.0.0</Link></div>
  </div>
</details>

<details id="element-can-be-located-by-selector-expression-with-no-timeout">
  <summary class="dsl">

  ```gherkin
  <element> can be located by <selector> "<expression>" with no <wait|timeout>
  ```

  <p>Binds a selector for a web element to the current scope with no lookup timeout.</p>
  </summary>
  <p>Where</p>
  <ul>
    <li><code>&lt;element&gt;</code> is the web element name</li>
    <li><code>&lt;selector&gt;</code> is the selector type, one of:
      <ul>
        <li><code>id</code> for selecting by id attribute</li>
        <li><code>name</code> for selecting by name attribute</li>
        <li><code>tag name</code> (or <code>tag</code> since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.46.0">v2.46.0</Link>) for selecting by tag name</li>
        <li><code>css selector</code> (or <code>css</code> since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.46.0">v2.46.0</Link>) for selecting by css expression</li>
        <li><code>class name</code> (or <code>class</code> since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.46.0">v2.46.0</Link>) for selecting by class attribute</li>
        <li><code>xpath</code> for selecting by XPath</li>
        <li><code>link text</code> for selecting by link text</li>
        <li><code>partial link text</code> for selecting by partial link text</li>
        <li><code>javascript</code> (or <code>js</code> since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.46.0">v2.46.0</Link>) for selecting <Link to="https://gweninterpreter.wordpress.com/2019/08/13/locating-web-elements-with-gwen/#byJavaScript">by JavaScript</Link></li>
      </ul>
    </li>
    <li><code>&lt;expression&gt;</code> is the selector expression</li>
    <li><code>&lt;wait|timeout&gt;</code> is either <code>wait</code> or <code>timeout</code></li>
  </ul>
  <p>Example</p>

  ```gherkin {1}
    Given the todo field can be located by css ".new-todo" with no lookup timeout
     When I enter "Walk the dog" in the todo field
  ```

  <div class="grid-3">
    <div><Link to="#element-can-be-located-by-selector-expression-with-no-timeout">Link</Link></div>
    <div align="center"><Link to="https://gweninterpreter.wordpress.com/2019/08/13/locating-web-elements-with-gwen/">Learn more</Link></div>
    <div align="right">Since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.19.0">v2.19.0</Link></div>
  </div>
</details>

<details id="element-can-be-located-by-selector-expression-with-timeout">
  <summary class="dsl">

  ```gherkin
  <element> can be located by <selector> "<expression>" with <timeoutSecs> second <wait|timeout>
  ```

  <p>Binds a selector for a web element to the current scope with a given lookup timeout.</p>
  </summary>
  <p>Where</p>
  <ul>
    <li><code>&lt;element&gt;</code> is the web element name</li>
    <li><code>&lt;selector&gt;</code> is the selector type, one of:
      <ul>
        <li><code>id</code> for selecting by id attribute</li>
        <li><code>name</code> for selecting by name attribute</li>
        <li><code>tag name</code> (or <code>tag</code> since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.46.0">v2.46.0</Link>) for selecting by tag name</li>
        <li><code>css selector</code> (or <code>css</code> since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.46.0">v2.46.0</Link>) for selecting by css expression</li>
        <li><code>class name</code> (or <code>class</code> since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.46.0">v2.46.0</Link>) for selecting by class attribute</li>
        <li><code>xpath</code> for selecting by XPath</li>
        <li><code>link text</code> for selecting by link text</li>
        <li><code>partial link text</code> for selecting by partial link text</li>
        <li><code>javascript</code> (or <code>js</code> since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.46.0">v2.46.0</Link>) for selecting <Link to="https://gweninterpreter.wordpress.com/2019/08/13/locating-web-elements-with-gwen/#byJavaScript">by JavaScript</Link></li>
      </ul>
    </li>
    <li><code>&lt;expression&gt;</code> is the selector expression</li>
    <li><code>&lt;timeoutSecs&gt;</code> is the timeout period in seconds</li>
    <li><code>&lt;wait|timeout&gt;</code> is either <code>wait</code> or <code>timeout</code></li>
  </ul>
  <p>Example</p>

  ```gherkin {1}
    Given the todo field can be located by css ".new-todo" with 5 second timeout
     When I enter "Walk the dog" in the todo field
  ```

  <div class="grid-3">
    <div><Link to="#element-can-be-located-by-selector-expression-with-timeout">Link</Link></div>
    <div align="center"><Link to="https://gweninterpreter.wordpress.com/2019/08/13/locating-web-elements-with-gwen/">Learn more</Link></div>
    <div align="right">Since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.19.0">v2.19.0</Link></div>
  </div>
</details>

<details id="element-can-be-located-by-selector-expression-relative-to-other-element">
  <summary class="dsl">

  ```gherkin
  <element> can be located by <selector> "<expression>" <relativeTo> <otherElement>
  ```

  <p>Binds a relative selector for a web element to the current scope with a default lookup timeout.</p>
  </summary>
  <p>Where</p>
  <ul>
    <li><code>&lt;element&gt;</code> is the web element name</li>
    <li><code>&lt;selector&gt;</code> is the selector type, one of:
      <ul>
        <li><code>id</code> for selecting by id attribute</li>
        <li><code>name</code> for selecting by name attribute</li>
        <li><code>tag name</code> (or <code>tag</code> since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.46.0">v2.46.0</Link>) for selecting by tag name</li>
        <li><code>css selector</code> (or <code>css</code> since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.46.0">v2.46.0</Link>) for selecting by css expression</li>
        <li><code>class name</code> (or <code>class</code> since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.46.0">v2.46.0</Link>) for selecting by class attribute</li>
        <li><code>xpath</code> for selecting by XPath</li>
        <li><code>link text</code> for selecting by link text</li>
        <li><code>partial link text</code> for selecting by partial link text</li>
        <li><code>javascript</code> (or <code>js</code> since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.46.0">v2.46.0</Link>) for selecting <Link to="https://gweninterpreter.wordpress.com/2019/08/13/locating-web-elements-with-gwen/#byJavaScript">by JavaScript</Link></li>
      </ul>
    </li>
    <li><code>&lt;expression&gt;</code> is the selector expression</li>
    <li><code>&lt;relativeTo&gt;</code> is the relationship to <code>&lt;otherElement&gt;</code>, one of:
      <ul>
        <li><code>in</code> for a contained relationship</li>
        <li><i>More relative options coming to Gwen 3</i></li>
      </ul>
    </li>
    <li><code>&lt;otherElement&gt;</code> is the name of the web element that this one is relative to</li>
  </ul>
  <p>Example</p>

  ```gherkin {2}
    Given the footer can be located by class "footer"
      And the displayed count can be located by tag "strong" in the footer
     Then the displayed count should be "3"
  ```

  <div class="grid-3">
    <div><Link to="#element-can-be-located-by-selector-expression-relative-to-other-element">Link</Link></div>
    <div align="center"><Link to="https://gweninterpreter.wordpress.com/2019/08/13/locating-web-elements-with-gwen/">Learn more</Link></div>
    <div align="right">Since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v1.1.0">v1.1.0</Link></div>
  </div>
</details>

<details id="element-can-be-located-by-selector-expression-relative-to-other-element-with-no-timeout">
  <summary class="dsl">

  ```gherkin
  <element> can be located by <selector> "<expression>" <relativeTo> <otherElement> with no <wait|timeout>
  ```

  <p>Binds a relative selector for a web element to the current scope with no lookup timeout.</p>
  </summary>
  <p>Where</p>
  <ul>
    <li><code>&lt;element&gt;</code> is the web element name</li>
    <li><code>&lt;selector&gt;</code> is the selector type, one of:
      <ul>
        <li><code>id</code> for selecting by id attribute</li>
        <li><code>name</code> for selecting by name attribute</li>
        <li><code>tag name</code> (or <code>tag</code> since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.46.0">v2.46.0</Link>) for selecting by tag name</li>
        <li><code>css selector</code> (or <code>css</code> since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.46.0">v2.46.0</Link>) for selecting by css expression</li>
        <li><code>class name</code> (or <code>class</code> since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.46.0">v2.46.0</Link>) for selecting by class attribute</li>
        <li><code>xpath</code> for selecting by XPath</li>
        <li><code>link text</code> for selecting by link text</li>
        <li><code>partial link text</code> for selecting by partial link text</li>
        <li><code>javascript</code> (or <code>js</code> since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.46.0">v2.46.0</Link>) for selecting <Link to="https://gweninterpreter.wordpress.com/2019/08/13/locating-web-elements-with-gwen/#byJavaScript">by JavaScript</Link></li>
      </ul>
    </li>
    <li><code>&lt;expression&gt;</code> is the selector expression</li>
    <li><code>&lt;relativeTo&gt;</code> is the relationship to <code>&lt;otherElement&gt;</code>, one of:
      <ul>
        <li><code>in</code> for a contained relationship</li>
        <li><i>More relative options coming to Gwen 3</i></li>
      </ul>
    </li>
    <li><code>&lt;otherElement&gt;</code> is the name of the web element that this one is relative to</li>
    <li><code>&lt;wait|timeout&gt;</code> is either <code>wait</code> or <code>timeout</code></li>
  </ul>
  <p>Example</p>

  ```gherkin {2}
    Given the footer can be located by class "footer"
      And the displayed count can be located by tag "strong" in the footer with no wait
     Then the displayed count should be "3"
  ```

  <div class="grid-3">
    <div><Link to="#element-can-be-located-by-selector-expression-relative-to-other-element-with-no-timeout">Link</Link></div>
    <div align="center"><Link to="https://gweninterpreter.wordpress.com/2019/08/13/locating-web-elements-with-gwen/">Learn more</Link></div>
    <div align="right">Since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.19.0">v2.19.0</Link></div>
  </div>
</details>

<details id="element-can-be-located-by-selector-expression-relative-to-other-element-with-timeout">
  <summary class="dsl">

  ```gherkin
  <element> can be located by <selector> "<expression>" <relativeTo> <otherElement>  with <timeoutSecs> second <wait|timeout>
  ```

  <p>Binds a relative selector for a web element to the current scope with a given lookup timeout.</p>
  </summary>
  <p>Where</p>
  <ul>
    <li><code>&lt;element&gt;</code> is the web element name</li>
    <li><code>&lt;selector&gt;</code> is the selector type, one of:
      <ul>
        <li><code>id</code> for selecting by id attribute</li>
        <li><code>name</code> for selecting by name attribute</li>
        <li><code>tag name</code> (or <code>tag</code> since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.46.0">v2.46.0</Link>) for selecting by tag name</li>
        <li><code>css selector</code> (or <code>css</code> since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.46.0">v2.46.0</Link>) for selecting by css expression</li>
        <li><code>class name</code> (or <code>class</code> since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.46.0">v2.46.0</Link>) for selecting by class attribute</li>
        <li><code>xpath</code> for selecting by XPath</li>
        <li><code>link text</code> for selecting by link text</li>
        <li><code>partial link text</code> for selecting by partial link text</li>
        <li><code>javascript</code> (or <code>js</code> since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.46.0">v2.46.0</Link>) for selecting <Link to="https://gweninterpreter.wordpress.com/2019/08/13/locating-web-elements-with-gwen/#byJavaScript">by JavaScript</Link></li>
      </ul>
    </li>
    <li><code>&lt;expression&gt;</code> is the selector expression</li>
    <li><code>&lt;relativeTo&gt;</code> is the relationship to <code>&lt;otherElement&gt;</code>, one of:
      <ul>
        <li><code>in</code> for a contained relationship</li>
        <li><i>More relative options coming to Gwen 3</i></li>
      </ul>
    </li>
    <li><code>&lt;otherElement&gt;</code> is the name of the web element that this one is relative to</li>
    <li><code>&lt;timeoutSecs&gt;</code> is the timeout period in seconds</li>
    <li><code>&lt;wait|timeout&gt;</code> is either <code>wait</code> or <code>timeout</code></li>
  </ul>
  <p>Example</p>

 ```gherkin {2}
    Given the footer can be located by class "footer"
      And the displayed count can be located by tag "strong" in the footer with no wait
     Then the displayed count should be "3"
  ```

  <div class="grid-3">
    <div><Link to="#element-can-be-located-by-selector-expression-relative-to-other-element-with-timeout">Link</Link></div>
    <div align="center"><Link to="https://gweninterpreter.wordpress.com/2019/08/13/locating-web-elements-with-gwen/">Learn more</Link></div>
    <div align="right">Since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.19.0">v2.19.0</Link></div>
  </div>
</details>

<details id="element-can-be-located-by-selector-expression-at-index">
  <summary class="dsl">

  ```gherkin
  <element> can be located by <selector> "<expression>" at index <index>
  ```

  <p>Binds an indexed selector for a web element to the current scope with a default lookup timeout.</p>
  </summary>
  <p>Where</p>
  <ul>
    <li><code>&lt;element&gt;</code> is the web element name</li>
    <li><code>&lt;selector&gt;</code> is the selector type, one of:
      <ul>
        <li><code>id</code> for selecting by id attribute</li>
        <li><code>name</code> for selecting by name attribute</li>
        <li><code>tag name</code> (or <code>tag</code> since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.46.0">v2.46.0</Link>) for selecting by tag name</li>
        <li><code>css selector</code> (or <code>css</code> since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.46.0">v2.46.0</Link>) for selecting by css expression</li>
        <li><code>class name</code> (or <code>class</code> since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.46.0">v2.46.0</Link>) for selecting by class attribute</li>
        <li><code>xpath</code> for selecting by XPath</li>
        <li><code>link text</code> for selecting by link text</li>
        <li><code>partial link text</code> for selecting by partial link text</li>
        <li><code>javascript</code> (or <code>js</code> since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.46.0">v2.46.0</Link>) for selecting <Link to="https://gweninterpreter.wordpress.com/2019/08/13/locating-web-elements-with-gwen/#byJavaScript">by JavaScript</Link></li>
      </ul>
    </li>
    <li><code>&lt;expression&gt;</code> is the selector expression</li>
    <li><code>&lt;index&gt;</code> is the index of the element to select in list returned by <code>&lt;expression&gt;</code></li>
  </ul>
  <p>Example</p>

  ```gherkin {1}
    Given the first item can be located by css ".todo-list label" at index 0
     Then the first item should contain "Walk the dog"
  ```

  <div class="grid-3">
    <div><Link to="#element-can-be-located-by-selector-expression-at-index">Link</Link></div>
    <div align="center"><Link to="https://gweninterpreter.wordpress.com/2019/08/13/locating-web-elements-with-gwen/">Learn more</Link></div>
    <div align="right">Since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.28.0">v2.28.0</Link></div>
  </div>
</details>

<details id="element-can-be-located-by-selector-expression-at-index-with-no-timeout">
  <summary class="dsl">

  ```gherkin
  <element> can be located by <selector> "<expression>" at index <index> with no <wait|timeout>
  ```

  <p>Binds an indexed selector for a web element to the current scope with no lookup timeout.</p>
  </summary>
  <p>Where</p>
  <ul>
    <li><code>&lt;element&gt;</code> is the web element name</li>
    <li><code>&lt;selector&gt;</code> is the selector type, one of:
      <ul>
        <li><code>id</code> for selecting by id attribute</li>
        <li><code>name</code> for selecting by name attribute</li>
        <li><code>tag name</code> (or <code>tag</code> since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.46.0">v2.46.0</Link>) for selecting by tag name</li>
        <li><code>css selector</code> (or <code>css</code> since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.46.0">v2.46.0</Link>) for selecting by css expression</li>
        <li><code>class name</code> (or <code>class</code> since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.46.0">v2.46.0</Link>) for selecting by class attribute</li>
        <li><code>xpath</code> for selecting by XPath</li>
        <li><code>link text</code> for selecting by link text</li>
        <li><code>partial link text</code> for selecting by partial link text</li>
        <li><code>javascript</code> (or <code>js</code> since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.46.0">v2.46.0</Link>) for selecting <Link to="https://gweninterpreter.wordpress.com/2019/08/13/locating-web-elements-with-gwen/#byJavaScript">by JavaScript</Link></li>
      </ul>
    </li>
    <li><code>&lt;expression&gt;</code> is the selector expression</li>
    <li><code>&lt;index&gt;</code> is the index of the element to select in list returned by <code>&lt;expression&gt;</code></li>
    <li><code>&lt;wait|timeout&gt;</code> is either <code>wait</code> or <code>timeout</code></li>
  </ul>
  <p>Example</p>

  ```gherkin {1}
    Given the first item can be located by css ".todo-list label" at index 0 with no lookup timeout
     Then the first item should contain "Walk the dog"
  ```

  <div class="grid-3">
    <div><Link to="#element-can-be-located-by-selector-expression-at-index-with-no-timeout">Link</Link></div>
    <div align="center"><Link to="https://gweninterpreter.wordpress.com/2019/08/13/locating-web-elements-with-gwen/">Learn more</Link></div>
    <div align="right">Since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.28.0">v2.28.0</Link></div>
  </div>
</details>

<details id="element-can-be-located-by-selector-expression-at-index-with-timeout">
  <summary class="dsl">

  ```gherkin
  <element> can be located by <selector> "<expression>" at index <index> with <timeoutSecs> second <wait|timeout>
  ```

  <p>Binds an indexed selector for a web element to the current scope with a given lookup timeout.</p>
  </summary>
  <p>Where</p>
  <ul>
    <li><code>&lt;element&gt;</code> is the web element name</li>
    <li><code>&lt;selector&gt;</code> is the selector type, one of:
      <ul>
        <li><code>id</code> for selecting by id attribute</li>
        <li><code>name</code> for selecting by name attribute</li>
        <li><code>tag name</code> (or <code>tag</code> since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.46.0">v2.46.0</Link>) for selecting by tag name</li>
        <li><code>css selector</code> (or <code>css</code> since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.46.0">v2.46.0</Link>) for selecting by css expression</li>
        <li><code>class name</code> (or <code>class</code> since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.46.0">v2.46.0</Link>) for selecting by class attribute</li>
        <li><code>xpath</code> for selecting by XPath</li>
        <li><code>link text</code> for selecting by link text</li>
        <li><code>partial link text</code> for selecting by partial link text</li>
        <li><code>javascript</code> (or <code>js</code> since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.46.0">v2.46.0</Link>) for selecting <Link to="https://gweninterpreter.wordpress.com/2019/08/13/locating-web-elements-with-gwen/#byJavaScript">by JavaScript</Link></li>
      </ul>
    </li>
    <li><code>&lt;expression&gt;</code> is the selector expression</li>
    <li><code>&lt;index&gt;</code> is the index of the element to select in list returned by <code>&lt;expression&gt;</code></li>
    <li><code>&lt;timeoutSecs&gt;</code> is the timeout period in seconds</li>
    <li><code>&lt;wait|timeout&gt;</code> is either <code>wait</code> or <code>timeout</code></li>
  </ul>
  <p>Example</p>

  ```gherkin {1}
    Given the first item can be located by css ".todo-list label" at index 0 with 5 second timeout
     Then the first item should contain "Walk the dog"
  ```

  <div class="grid-3">
    <div><Link to="#element-can-be-located-by-selector-expression-at-index-with-timeout">Link</Link></div>
    <div align="center"><Link to="https://gweninterpreter.wordpress.com/2019/08/13/locating-web-elements-with-gwen/">Learn more</Link></div>
    <div align="right">Since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.28.0">v2.28.0</Link></div>
  </div>
</details>

<details id="element-can-be-located-by-selector-expression-relative-to-other-element-at-index">
  <summary class="dsl">

  ```gherkin
  <element> can be located by <selector> "<expression>" at index <index> <relativeTo> <otherElement>
  ```

  <p>Binds a relative selector for a web element to the current scope with a default lookup timeout.</p>
  </summary>
  <p>Where</p>
  <ul>
    <li><code>&lt;element&gt;</code> is the web element name</li>
    <li><code>&lt;selector&gt;</code> is the selector type, one of:
      <ul>
        <li><code>id</code> for selecting by id attribute</li>
        <li><code>name</code> for selecting by name attribute</li>
        <li><code>tag name</code> (or <code>tag</code> since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.46.0">v2.46.0</Link>) for selecting by tag name</li>
        <li><code>css selector</code> (or <code>css</code> since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.46.0">v2.46.0</Link>) for selecting by css expression</li>
        <li><code>class name</code> (or <code>class</code> since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.46.0">v2.46.0</Link>) for selecting by class attribute</li>
        <li><code>xpath</code> for selecting by XPath</li>
        <li><code>link text</code> for selecting by link text</li>
        <li><code>partial link text</code> for selecting by partial link text</li>
        <li><code>javascript</code> (or <code>js</code> since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.46.0">v2.46.0</Link>) for selecting <Link to="https://gweninterpreter.wordpress.com/2019/08/13/locating-web-elements-with-gwen/#byJavaScript">by JavaScript</Link></li>
      </ul>
    </li>
    <li><code>&lt;expression&gt;</code> is the selector expression</li>
    <li><code>&lt;index&gt;</code> is the index of the element to select in list returned by <code>&lt;expression&gt;</code></li>
    <li><code>&lt;relativeTo&gt;</code> is the relationship to <code>&lt;otherElement&gt;</code>, one of:
      <ul>
        <li><code>in</code> for a contained relationship</li>
        <li><i>More relative options coming to Gwen 3</i></li>
      </ul>
    </li>
    <li><code>&lt;otherElement&gt;</code> is the name of the web element that this one is relative to</li>
  </ul>
  <p>Example</p>

  ```gherkin {2}
    Given the count info can be located by class "todo-count"
      And the count unit can be located by tag "span" at index 1 in the count info
     Then the count unit should contain "item"
  ```

  <div class="grid-3">
    <div><Link to="#element-can-be-located-by-selector-expression-relative-to-other-element-at-index">Link</Link></div>
    <div align="center"><Link to="https://gweninterpreter.wordpress.com/2019/08/13/locating-web-elements-with-gwen/">Learn more</Link></div>
    <div align="right">Since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.28.0">v2.28.0</Link></div>
  </div>
</details>

<details id="element-can-be-located-by-selector-expression-at-index-relative-to-other-element-with-no-timeout">
  <summary class="dsl">

  ```gherkin
  <element> can be located by <selector> "<expression>" at index <index> <relativeTo> <otherElement> with no <wait|timeout>
  ```

  <p>Binds a relative indexed selector for a web element to the current scope with no lookup timeout.</p>
  </summary>
  <p>Where</p>
  <ul>
    <li><code>&lt;element&gt;</code> is the web element name</li>
    <li><code>&lt;selector&gt;</code> is the selector type, one of:
      <ul>
        <li><code>id</code> for selecting by id attribute</li>
        <li><code>name</code> for selecting by name attribute</li>
        <li><code>tag name</code> (or <code>tag</code> since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.46.0">v2.46.0</Link>) for selecting by tag name</li>
        <li><code>css selector</code> (or <code>css</code> since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.46.0">v2.46.0</Link>) for selecting by css expression</li>
        <li><code>class name</code> (or <code>class</code> since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.46.0">v2.46.0</Link>) for selecting by class attribute</li>
        <li><code>xpath</code> for selecting by XPath</li>
        <li><code>link text</code> for selecting by link text</li>
        <li><code>partial link text</code> for selecting by partial link text</li>
        <li><code>javascript</code> (or <code>js</code> since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.46.0">v2.46.0</Link>) for selecting <Link to="https://gweninterpreter.wordpress.com/2019/08/13/locating-web-elements-with-gwen/#byJavaScript">by JavaScript</Link></li>
      </ul>
    </li>
    <li><code>&lt;expression&gt;</code> is the selector expression</li>
    <li><code>&lt;index&gt;</code> is the index of the element to select in list returned by <code>&lt;expression&gt;</code></li>
    <li><code>&lt;relativeTo&gt;</code> is the relationship to <code>&lt;otherElement&gt;</code>, one of:
      <ul>
        <li><code>in</code> for a contained relationship</li>
        <li><i>More relative options coming to Gwen 3</i></li>
      </ul>
    </li>
    <li><code>&lt;otherElement&gt;</code> is the name of the web element that this one is relative to</li>
    <li><code>&lt;wait|timeout&gt;</code> is either <code>wait</code> or <code>timeout</code></li>
  </ul>
  <p>Example</p>

  ```gherkin {2}
    Given the count info can be located by class "todo-count"
      And the count unit can be located by tag "span" at index 1 in the count info with no wait
     Then the count unit should contain "item"
  ```

  <div class="grid-3">
    <div><Link to="#element-can-be-located-by-selector-expression-at-index-relative-to-other-element-with-no-timeout">Link</Link></div>
    <div align="center"><Link to="https://gweninterpreter.wordpress.com/2019/08/13/locating-web-elements-with-gwen/">Learn more</Link></div>
    <div align="right">Since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.28.0">v2.28.0</Link></div>
  </div>
</details>

<details id="element-can-be-located-by-selector-expression-at-index-relative-to-other-element-with-timeout">
  <summary class="dsl">

  ```gherkin
  <element> can be located by <selector> "<expression>" at index <index> <relativeTo> <otherElement>  with <timeoutSecs> second <wait|timeout>
  ```

  <p>Binds a relative indexed selector for a web element to the current scope with a given lookup timeout.</p>
  </summary>
  <p>Where</p>
  <ul>
    <li><code>&lt;element&gt;</code> is the web element name</li>
    <li><code>&lt;selector&gt;</code> is the selector type, one of:
      <ul>
        <li><code>id</code> for selecting by id attribute</li>
        <li><code>name</code> for selecting by name attribute</li>
        <li><code>tag name</code> (or <code>tag</code> since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.46.0">v2.46.0</Link>) for selecting by tag name</li>
        <li><code>css selector</code> (or <code>css</code> since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.46.0">v2.46.0</Link>) for selecting by css expression</li>
        <li><code>class name</code> (or <code>class</code> since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.46.0">v2.46.0</Link>) for selecting by class attribute</li>
        <li><code>xpath</code> for selecting by XPath</li>
        <li><code>link text</code> for selecting by link text</li>
        <li><code>partial link text</code> for selecting by partial link text</li>
        <li><code>javascript</code> (or <code>js</code> since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.46.0">v2.46.0</Link>) for selecting <Link to="https://gweninterpreter.wordpress.com/2019/08/13/locating-web-elements-with-gwen/#byJavaScript">by JavaScript</Link></li>
      </ul>
    </li>
    <li><code>&lt;expression&gt;</code> is the selector expression</li>
    <li><code>&lt;index&gt;</code> is the index of the element to select in list returned by <code>&lt;expression&gt;</code></li>
    <li><code>&lt;relativeTo&gt;</code> is the relationship to <code>&lt;otherElement&gt;</code>, one of:
      <ul>
        <li><code>in</code> for a contained relationship</li>
        <li><i>More relative options coming to Gwen 3</i></li>
      </ul>
    </li>
    <li><code>&lt;otherElement&gt;</code> is the name of the web element that this one is relative to</li>
    <li><code>&lt;timeoutSecs&gt;</code> is the timeout period in seconds</li>
    <li><code>&lt;wait|timeout&gt;</code> is either <code>wait</code> or <code>timeout</code></li>
  </ul>
  <p>Example</p>

 ```gherkin {2}
    Given the count info can be located by class "todo-count"
      And the count unit can be located by tag "span" at index 1 in the count info with 5 second wait
     Then the count unit should contain "item"
  ```

  <div class="grid-3">
    <div><Link to="#element-can-be-located-by-selector-expression-at-index-relative-to-other-element-with-timeout">Link</Link></div>
    <div align="center"><Link to="https://gweninterpreter.wordpress.com/2019/08/13/locating-web-elements-with-gwen/">Learn more</Link></div>
    <div align="right">Since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.28.0">v2.28.0</Link></div>
  </div>
</details>

<details id="element-can-be-located-by-selector-with-fallbacks">
  <summary class="dsl">

  ```gherkin
  <element> can be located by
    | <selector 1> | <expression 1> |
    | <selector 2> | <expression 2> |
    |      ..      |       ..       |
    | <selector n> | <expression n> |
  ```

  <p>
    Binds primary and fallback selectors for a web element to the current scope with a default lookup timeout.
    Lookups will be attempted in the order specified until one succeeds.
  </p>
  </summary>
  <p>Where</p>
  <ul>
    <li><code>&lt;element&gt;</code> is the web element name</li>
    <li><code>&lt;selector n&gt;</code> is the nth selector type, one of:
      <ul>
        <li><code>id</code> for selecting by id attribute</li>
        <li><code>name</code> for selecting by name attribute</li>
        <li><code>tag name</code> (or <code>tag</code> since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.46.0">v2.46.0</Link>) for selecting by tag name</li>
        <li><code>css selector</code> (or <code>css</code> since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.46.0">v2.46.0</Link>) for selecting by css expression</li>
        <li><code>class name</code> (or <code>class</code> since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.46.0">v2.46.0</Link>) for selecting by class attribute</li>
        <li><code>xpath</code> for selecting by XPath</li>
        <li><code>link text</code> for selecting by link text</li>
        <li><code>partial link text</code> for selecting by partial link text</li>
        <li><code>javascript</code> (or <code>js</code> since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.46.0">v2.46.0</Link>) for selecting <Link to="https://gweninterpreter.wordpress.com/2019/08/13/locating-web-elements-with-gwen/#byJavaScript">by JavaScript</Link></li>
      </ul>
    </li>
    <li><code>&lt;expression n&gt;</code> is the nth selector expression</li>
  </ul>
  <p>Example</p>

  ```gherkin {1-4}
    Given the todo field can be located by
          | name  | todo-field  |
          | css   | .todo input |
          | class | new-todo    |
     When I type "Feed the cat" in the todo field
  ```

  <div class="grid-3">
    <div><Link to="#element-can-be-located-by-selector-with-fallbacks">Link</Link></div>
    <div align="center"><Link to="https://gweninterpreter.wordpress.com/2019/08/13/locating-web-elements-with-gwen/">Learn more</Link></div>
    <div align="right">Since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.11.0">v2.11.0</Link></div>
  </div>
</details>

<details id="element-can-be-located-by-selector-with-fallbacks-at-index">
  <summary class="dsl">

  ```gherkin
  <element> can be located at index <index> by
    | <selector 1> | <expression 1> |
    | <selector 2> | <expression 2> |
    |      ..      |       ..       |
    | <selector n> | <expression n> |
  ```

  <p>
    Binds indexed primary and fallback selectors for a web element to the current scope with a default lookup timeout.
    Lookups will be attempted in the order specified until one succeeds.
  </p>
  </summary>
  <p>Where</p>
  <ul>
    <li><code>&lt;element&gt;</code> is the web element name</li>
    <li><code>&lt;selector n&gt;</code> is the nth selector type, one of:
      <ul>
        <li><code>id</code> for selecting by id attribute</li>
        <li><code>name</code> for selecting by name attribute</li>
        <li><code>tag name</code> (or <code>tag</code> since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.46.0">v2.46.0</Link>) for selecting by tag name</li>
        <li><code>css selector</code> (or <code>css</code> since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.46.0">v2.46.0</Link>) for selecting by css expression</li>
        <li><code>class name</code> (or <code>class</code> since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.46.0">v2.46.0</Link>) for selecting by class attribute</li>
        <li><code>xpath</code> for selecting by XPath</li>
        <li><code>link text</code> for selecting by link text</li>
        <li><code>partial link text</code> for selecting by partial link text</li>
        <li><code>javascript</code> (or <code>js</code> since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.46.0">v2.46.0</Link>) for selecting <Link to="https://gweninterpreter.wordpress.com/2019/08/13/locating-web-elements-with-gwen/#byJavaScript">by JavaScript</Link></li>
      </ul>
    </li>
    <li><code>&lt;expression n&gt;</code> is the nth selector expression</li>
    <li><code>&lt;index&gt;</code> is the index of the element to select in list returned by <code>&lt;expression n&gt;</code></li>
  </ul>
  <p>Example</p>

  ```gherkin {1-4}
    Given the second item can be located at index 1 by
          | name  | todo-label       |
          | class | todo-label       |
          | css   | .todo-list label |
     Then the second item should contain "Feed the cat"
  ```

  <div class="grid-3">
    <div><Link to="#element-can-be-located-by-selector-with-fallbacks-at-index">Link</Link></div>
    <div align="center"><Link to="https://gweninterpreter.wordpress.com/2019/08/13/locating-web-elements-with-gwen/">Learn more</Link></div>
    <div align="right">Since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.28.0">v2.28.0</Link></div>
  </div>
</details>

<details id="element-can-be-located-by-selector-with-fallbacks-at-index-with-no-timeout">
  <summary class="dsl">

  ```gherkin
  <element> can be located at index <index> with no <wait|timeout> by
    | <selector 1> | <expression 1> |
    | <selector 2> | <expression 2> |
    |      ..      |       ..       |
    | <selector n> | <expression n> |
  ```

  <p>
    Binds indexed primary and fallback selectors for a web element to the current scope with no lookup timeout.
    Lookups will be attempted in the order specified until one succeeds.
  </p>
  </summary>
  <p>Where</p>
  <ul>
    <li><code>&lt;element&gt;</code> is the web element name</li>
    <li><code>&lt;selector n&gt;</code> is the nth selector type, one of:
      <ul>
        <li><code>id</code> for selecting by id attribute</li>
        <li><code>name</code> for selecting by name attribute</li>
        <li><code>tag name</code> (or <code>tag</code> since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.46.0">v2.46.0</Link>) for selecting by tag name</li>
        <li><code>css selector</code> (or <code>css</code> since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.46.0">v2.46.0</Link>) for selecting by css expression</li>
        <li><code>class name</code> (or <code>class</code> since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.46.0">v2.46.0</Link>) for selecting by class attribute</li>
        <li><code>xpath</code> for selecting by XPath</li>
        <li><code>link text</code> for selecting by link text</li>
        <li><code>partial link text</code> for selecting by partial link text</li>
        <li><code>javascript</code> (or <code>js</code> since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.46.0">v2.46.0</Link>) for selecting <Link to="https://gweninterpreter.wordpress.com/2019/08/13/locating-web-elements-with-gwen/#byJavaScript">by JavaScript</Link></li>
      </ul>
    </li>
    <li><code>&lt;expression n&gt;</code> is the nth selector expression</li>
    <li><code>&lt;index&gt;</code> is the index of the element to select in list returned by <code>&lt;expression n&gt;</code></li>
    <li><code>&lt;wait|timeout&gt;</code> is either <code>wait</code> or <code>timeout</code></li>
  </ul>
  <p>Example</p>

  ```gherkin {1-4}
    Given the second item can be located at index 1 with no lookup timeout by
          | name  | todo-label       |
          | class | todo-label       |
          | css   | .todo-list label |
     Then the second item should contain "Feed the cat"
  ```

  <div class="grid-3">
    <div><Link to="#element-can-be-located-by-selector-with-fallbacks-at-index-with-no-timeout">Link</Link></div>
    <div align="center"><Link to="https://gweninterpreter.wordpress.com/2019/08/13/locating-web-elements-with-gwen/">Learn more</Link></div>
    <div align="right">Since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.28.0">v2.28.0</Link></div>
  </div>
</details>

<details id="element-can-be-located-by-selector-with-fallbacks-at-index-with-timeout">
  <summary class="dsl">

  ```gherkin
  <element> can be located at index <index> with <timeoutSecs> second <wait|timeout> by
    | <selector 1> | <expression 1> |
    | <selector 2> | <expression 2> |
    |      ..      |       ..       |
    | <selector n> | <expression n> |
  ```

  <p>
    Binds indexed primary and fallback selectors for a web element to the current scope with a given lookup timeout.
    Lookups will be attempted in the order specified until one succeeds.
  </p>
  </summary>
  <p>Where</p>
  <ul>
    <li><code>&lt;element&gt;</code> is the web element name</li>
    <li><code>&lt;selector n&gt;</code> is the nth selector type, one of:
      <ul>
        <li><code>id</code> for selecting by id attribute</li>
        <li><code>name</code> for selecting by name attribute</li>
        <li><code>tag name</code> (or <code>tag</code> since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.46.0">v2.46.0</Link>) for selecting by tag name</li>
        <li><code>css selector</code> (or <code>css</code> since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.46.0">v2.46.0</Link>) for selecting by css expression</li>
        <li><code>class name</code> (or <code>class</code> since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.46.0">v2.46.0</Link>) for selecting by class attribute</li>
        <li><code>xpath</code> for selecting by XPath</li>
        <li><code>link text</code> for selecting by link text</li>
        <li><code>partial link text</code> for selecting by partial link text</li>
        <li><code>javascript</code> (or <code>js</code> since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.46.0">v2.46.0</Link>) for selecting <Link to="https://gweninterpreter.wordpress.com/2019/08/13/locating-web-elements-with-gwen/#byJavaScript">by JavaScript</Link></li>
      </ul>
    </li>
    <li><code>&lt;expression n&gt;</code> is the nth selector expression</li>
    <li><code>&lt;index&gt;</code> is the index of the element to select in list returned by <code>&lt;expression n&gt;</code></li>
    <li><code>&lt;timeoutSecs&gt;</code> is the timeout period in seconds</li>
    <li><code>&lt;wait|timeout&gt;</code> is either <code>wait</code> or <code>timeout</code></li>
  </ul>
  <p>Example</p>

  ```gherkin {1-4}
    Given the second item can be located at index 1 with 5 second timeout by
          | name  | todo-label       |
          | class | todo-label       |
          | css   | .todo-list label |
     Then the second item should contain "Feed the cat"
  ```

  <div class="grid-3">
    <div><Link to="#element-can-be-located-by-selector-with-fallbacks-at-index-with-timeout">Link</Link></div>
    <div align="center"><Link to="https://gweninterpreter.wordpress.com/2019/08/13/locating-web-elements-with-gwen/">Learn more</Link></div>
    <div align="right">Since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.28.0">v2.28.0</Link></div>
  </div>
</details>

<details id="element-can-be-actioned-by-javascript">
  <summary class="dsl">

  ```gherkin
  <element> can be <actioned> by <javascript|js> "<javascript>"
  ```
  <p>Registers a javascript action on an element. When the action is performed, then the javascript will execute in place of the default behavior.</p>
  </summary>
  <p>Where</p>
  <ul>
    <li><code>&lt;element&gt;</code> is the name of the element to register the action on</li>
    <li><code>&lt;actioned&gt;</code> is one of:
      <ul>
        <li><code>clicked</code> to perform the action when the element is clicked</li>
        <li><code>right clicked</code> to perform the action when the element is right clicked, since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.8.0">v2.8.0</Link></li>
        <li><code>double clicked</code> to perform the action when the element is double clicked, since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.12.0">v2.12.0</Link></li>
        <li><code>submitted</code> to perform the action when the form element is submitted</li>
        <li><code>checked</code> (or <code>ticked</code> since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.4.0">v2.4.0</Link>) to perform the action when the checkbox element is ticked</li>
        <li><code>unchecked</code> (or <code>unticked</code> since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.4.0">v2.4.0</Link>) to perform the action when the checkbox element is unticked</li>
        <li><code>selected</code> to perform the action when a dropdown option is selected in the element</li>
        <li><code>deselected</code> to perform the action when a mutli-select dropdown option is deselected in the element, since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.13.0">v2.13.0</Link></li>
        <li><code>typed</code> to perform the action when text is typed into the element</li>
        <li><code>entered</code> to perform the action when text is entered into the element</li>
        <li><code>tabbed</code> to perform the action when the tab key is pressed in the element, since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.17.1">v2.17.1</Link></li>
        <li><code>cleared</code> to perform the action when the text is cleared in the element</li>
        <li><code>moved to</code> to perform the action when the focus is moved to the element</li>
      </ul>
    </li>
    <li><code>&lt;javascript|js&gt;</code> is <code>javascript</code> (or <code>js</code> , since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.46.0">v2.46.0</Link>)</li>
    <li><code>&lt;javascript&gt;</code> is the javascript expression or anonymous function that will perform the desired action (can be in <Link to="https://github.com/gwen-interpreter/gwen/wiki/Doc-Strings">DocString</Link> position)
      <ul>
        <li>The reference to the element will be provided to the javascript through an implicit object named <code>element</code>.</li>
        <li>For an expression: no <code>return</code> prefix is required</li>
        <li>For an anonymous function: the syntax is <code>(function()&#123; body &#125;)()</code> where body must return a value</li>
      </ul>
    </li>
  </ul>
  <p>Example</p>

  ```gherkin {2}
    Given the submit button can be located by css ".submit"
      And the submit button can be clicked by js "element.click()"
     When I click the submit button
  ```

  <div class="grid-3">
    <div><Link to="#element-can-be-actioned-by-javascript">Link</Link></div>
    <div align="center"></div>
    <div align="right">Since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v1.8.0">v1.8.0</Link></div>
  </div>
</details>

### When

<details id="i-click-element">
  <summary class="dsl">

  ```gherkin
  I <click|right click|double click> <element>
  ```
  <p>Clicks a web element.</p>
  </summary>
  <p>Where</p>
  <ul>
    <li><code>&lt;click|right click|double click&gt;</code> is one of:
      <ul>
        <li><code>click</code> to click the element</li>
        <li><code>right click</code> to right click the element, since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.8.0">v2.8.0</Link></li>
        <li><code>double click</code> to double click the element, since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.12.0">v2.12.0</Link></li>
      </ul>
    </li>
    <li><code>&lt;element&gt;</code> is the name of the element to click</li>
  </ul>
  <p>Example</p>

  ```gherkin {3}
    Given the start button can be located by name "commit"
     When I navigate to "https://challengers.flood.io/start"
      And I click the start button
  ```

  <div class="grid-3">
    <div><Link to="#i-click-element">Link</Link></div>
    <div align="center"></div>
    <div align="right">Since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v1.0.0">v1.0.0</Link></div>
  </div>
</details>

<details id="i-click-element-of-context">
  <summary class="dsl">

  ```gherkin
  I <click|right click|double click> <element> of <context>
  ```
  <p>Clicks a context sensitive web element.</p>
  </summary>
  <p>Where</p>
  <ul>
    <li><code>&lt;click|right click|double click&gt;</code> is one of:
      <ul>
        <li><code>click</code> to click the element</li>
        <li><code>right click</code> to right click the element, since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.8.0">v2.8.0</Link></li>
        <li><code>double click</code> to double click the element, since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.12.0">v2.12.0</Link></li>
      </ul>
    </li>
    <li><code>&lt;element&gt;</code> is the name of the element to click</li>
    <li><code>&lt;context&gt;</code> is the context element to focus into (in order to click <code>&lt;element&gt;</code>)</li>
  </ul>
  <p>Example</p>

  ```gherkin {3}
    Given the menu item can be located by id "menu-item-id"
      And the action button can be located by id "action-button-id"
     When I click the action button of the menu item
  ```

  <div class="grid-3">
    <div><Link to="#i-click-element-of-context">Link</Link></div>
    <div align="center"></div>
    <div align="right">Since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v1.2.0">v1.2.0</Link></div>
  </div>
</details>

<details id="i-modifier-keys-click-element">
  <summary class="dsl">

  ```gherkin
  I <modifierKeys> <click|right click|double click> <element>
  ```
  <p>Holds down the specified modifier keys and clicks an element.</p>
  </summary>
  <p>Where</p>
  <ul>
    <li><code>&lt;modifierKeys&gt;</code> is a <code>+</code> separated list of modifier keys to hold down during the click
      <ul>
        <li>Modifier keys can be single keyboard characters or any supported <Link to="https://seleniumhq.github.io/selenium/docs/api/java/org/openqa/selenium/Keys.html">key constants</Link></li>
        <li><i>Node:</i> Keys behave differently on different platforms</li>
      </ul>
    </li>
    <li><code>&lt;click|right click|double click&gt;</code> is one of:
      <ul>
        <li><code>click</code> to click the element</li>
        <li><code>right click</code> to right click the element</li>
        <li><code>double click</code> to double click the element, since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.12.0">v2.12.0</Link></li>
      </ul>
    </li>
    <li><code>&lt;element&gt;</code> is the name of the element to click</li>
  </ul>
  <p>Example</p>

  ```gherkin {2}
    Given the button can be located by tag "button"
      And I COMMAND+SHIFT click the button
  ```

  <div class="grid-3">
    <div><Link to="#i-modifier-keys-click-element">Link</Link></div>
    <div align="center"></div>
    <div align="right">Since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.8.0">v2.8.0</Link></div>
  </div>
</details>

<details id="i-tick-or-until-checkbox">
  <summary class="dsl">

  ```gherkin
  I <check|uncheck|tick|untick> <checkbox>
  ```
  <p>Ticks or unticks a checkbox element.</p>
  </summary>
  <p>Where</p>
  <ul>
    <li><code>&lt;check|uncheck|tick|untick&gt;</code> is one of:
      <ul>
        <li><code>check</code> (or <code>tick</code> since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.4.0">v2.4.0</Link>) to tick the checkbox element</li>
        <li><code>uncheck</code> (or <code>untick</code> since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.4.0">v2.4.0</Link>) to untick the checkbox element</li>
      </ul>
    </li>
    <li><code>&lt;checkbox&gt;</code> is the name of the checkbox element</li>
  </ul>
  <p>Example</p>

  ```gherkin {3}
    Given the email checkbox can be located by id "checkbox1"
     When I navigate to "https://automationintesting.com/selenium/testpage"
      And I tick the email checkbox
  ```

  <div class="grid-3">
    <div><Link to="#i-tick-or-until-checkbox">Link</Link></div>
    <div align="center"></div>
    <div align="right">Since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v1.0.0">v1.0.0</Link></div>
  </div>
</details>

<details id="i-submit-form">
  <summary class="dsl">

  ```gherkin
  I submit <form>
  ```
  <p>Submits a web form.</p>
  </summary>
  <p>Where</p>
  <ul>
    <li><code>&lt;form&gt;</code> is the name of the form to submit</li>
  </ul>
  <p>Example</p>

  ```gherkin {3}
    Given the form can be located by name "contactform"
     When I navigate to "https://automationintesting.com/selenium/testpage"
      And I submit the form
  ```

  <div class="grid-3">
    <div><Link to="#i-submit-form">Link</Link></div>
    <div align="center"></div>
    <div align="right">Since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v1.0.0">v1.0.0</Link></div>
  </div>
</details>

<details id="i-move-to-element">
  <summary class="dsl">

  ```gherkin
  I move to <element>
  ```
  <p>Moves to a web element and gives it the focus.</p>
  </summary>
  <p>Where</p>
  <ul>
    <li><code>&lt;element&gt;</code> is the name of the element to move to</li>
  </ul>
  <p>Example</p>

  ```gherkin {3}
    Given the surname field can be located by id "surname"
     When I navigate to "https://automationintesting.com/selenium/testpage"
      And I move to the surname field
  ```

  <div class="grid-3">
    <div><Link to="#i-move-to-element">Link</Link></div>
    <div align="center"></div>
    <div align="right">Since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v1.0.0">v1.0.0</Link></div>
  </div>
</details>

<details id="i-enter-or-type-text-in-element">
  <summary class="dsl">

  ```gherkin
  I <enter|type> "<text>" in <element>
  ```
  <p>Enters or types text into a web element.</p>
  </summary>
  <p>Where</p>
  <ul>
    <li><code>&lt;enter|type&gt;</code> is one of:
      <ul>
        <li><code>enter</code> to send the text to the field followed by the enter key</li>
        <li><code>type</code> to send the text to the field only (without trailing enter key)</li>
      </ul>
    </li>
    <li><code>&lt;text&gt;</code> is the text to send to the field</li>
    <li><code>&lt;element&gt;</code> is the name of the field to send the text to</li>
  </ul>
  <p>Example</p>

  ```gherkin {3}
    Given the todo field can be located by class "new-todo"
     When I navigate to "https://todomvc.com/examples/react"
      And I enter "Walk the dog" in the todo field
  ```

  <div class="grid-3">
    <div><Link to="#i-enter-or-type-text-in-element">Link</Link></div>
    <div align="center"></div>
    <div align="right">Since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v1.0.0">v1.0.0</Link></div>
  </div>
</details>

<details id="i-enter-or-type-text-ref-in-element">
  <summary class="dsl">

  ```gherkin
  I <enter|type> <textRef> in <element>
  ```
  <p>Enters or types a referenced text value into a web element.</p>
  </summary>
  <p>Where</p>
  <ul>
    <li><code>&lt;enter|type&gt;</code> is one of:
      <ul>
        <li><code>enter</code> to send the text to the field followed by the enter key</li>
        <li><code>type</code> to send the text to the field only (without trailing enter key)</li>
      </ul>
    </li>
    <li><code>&lt;textRef&gt;</code> is the name of the binding containing the text to send to the field
      <ul>
        <li>Can be the name of any binding that contains or resolves to a text value, including a web element in which case the text in the element will be dynamically retrieved and used.</li>
      </ul>
    </li>
    <li><code>&lt;element&gt;</code> is the name of the field to send the text to</li>
  </ul>
  <p>Example</p>

  ```gherkin {3}
    Given the todo field can be located by class "new-todo"
      And my item is "Walk the dog"
     When I navigate to "https://todomvc.com/examples/react"
      And I enter my item in the todo field
  ```

  <div class="grid-3">
    <div><Link to="#i-enter-or-type-text-ref-in-element">Link</Link></div>
    <div align="center"></div>
    <div align="right">Since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v1.0.0">v1.0.0</Link></div>
  </div>
</details>

<details id="i-append-text-to-element">
  <summary class="dsl">

  ```gherkin
  I append "<text>" to <element>
  ```
  <p>Appends the given text to the current text value of an element.</p>
  </summary>
  <p>Where</p>
  <ul>
    <li><code>&lt;text&gt;</code> is the text to append</li>
    <li><code>&lt;element&gt;</code> is name of the element to append the text to</li>
  </ul>
  <p>Example</p>

  ```gherkin {4}
    Given the search field can be located by name "q"
     When I navigate to "https://google.com"
      And I type "Gwen" in the search field
      And I append " automation" to the search field
  ```

  <div class="grid-3">
    <div><Link to="#i-append-text-to-element">Link</Link></div>
    <div align="center"></div>
    <div align="right">Since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.18.0">v2.18.0</Link></div>
  </div>
</details>

<details id="i-append-text-ref-to-element">
  <summary class="dsl">

  ```gherkin
  I append <textRef> to <element>
  ```
  <p>Appends the given referenced text to the current text value of an element.</p>
  </summary>
  <p>Where</p>
  <ul>
    <li><code>&lt;textRef&gt;</code> is the name of the binding containing the text to append to the element
      <ul>
        <li>Can be the name of any binding that contains or resolves to a text value, including a web element in which case the text in the element will be dynamically retrieved and used.</li>
      </ul>
    </li>
    <li><code>&lt;element&gt;</code> is name of the element to append the text to</li>
  </ul>
  <p>Example</p>

  ```gherkin {5}
    Given the search field can be located by name "q"
      And my additional search term is " automation"
     When I navigate to "https://google.com"
      And I type "Gwen" in the search field
      And I append my additional search term to the search field
  ```

  <div class="grid-3">
    <div><Link to="#i-append-text-ref-to-element">Link</Link></div>
    <div align="center"></div>
    <div align="right">Since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.18.0">v2.18.0</Link></div>
  </div>
</details>

<details id="i-press-enter-or-tab-in-element">
  <summary class="dsl">

  ```gherkin
  I press <enter|tab> in <element>
  ```
  <p>Send the enter or tab key to a web element.</p>
  </summary>
  <p>Where</p>
  <ul>
    <li><code>&lt;enter|tab&gt;</code> is one of:
      <ul>
        <li><code>enter</code> to send the enter key</li>
        <li><code>tab</code> to send the tab key</li>
      </ul>
    </li>
    <li><code>&lt;element&gt;</code> is the name of the element to send the key to</li>
  </ul>
  <p>Example</p>

  ```gherkin {3}
    Given the surname field can be located by id "surname"
     When I navigate to "https://automationintesting.com/selenium/testpage"
      And I press tab in the surname field
  ```

  <div class="grid-3">
    <div><Link to="#i-press-enter-or-tab-in-element">Link</Link></div>
    <div align="center"></div>
    <div align="right">Since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v1.0.0">v1.0.0</Link></div>
  </div>
</details>

<details id="i-send-keys-to-element">
  <summary class="dsl">

  ```gherkin
  I send "<keys>" to <element>
  ```
  <p>Sends a sequence of keys to an element (for emulating keyboard shortcuts).</p>
  </summary>
  <p>Where</p>
  <ul>
    <li><code>&lt;keys&gt;</code> is a <code>,</code> or <code>+</code> (since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.53.0">v2.53.0</Link>) separated list of keys to send.
      <ul>
        <li>Keys can be single keyboard characters or any supported <Link to="https://seleniumhq.github.io/selenium/docs/api/java/org/openqa/selenium/Keys.html">key constants</Link></li>
        <li><i>Note:</i> Keys behave differently on different platforms</li>
      </ul>
    </li>
    <li><code>&lt;element&gt;</code> is the name of the element to send the keys to</li>
  </ul>
  <p>Example</p>

  ```gherkin {4,5,7,8}
    Given the search field can be located by name "q"
     When I navigate to "https://google.com"
      And I type "Gwen automation" in the search field
      And I send "COMMAND+A" to the search field
      And I send "COMMAND+C" to the search field
      And I clear the search field
      And I send "COMMAND+V" to the search field
      And I send "ARROW_DOWN" to the search field
      And I press enter in the search field
  ```

  <div class="grid-3">
    <div><Link to="#i-send-keys-to-element">Link</Link></div>
    <div align="center"></div>
    <div align="right">Since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.8.0">v2.8.0</Link></div>
  </div>
</details>

<details id="i-insert-a-new-line-in-textarea">
  <summary class="dsl">

  ```gherkin
  I insert a new line in <textarea>
  ```
  <p>Inserts a new line into a textarea element.</p>
  </summary>
  <p>Where</p>
  <ul>
    <li><code>&lt;textarea&gt;</code> is the name of the textarea element to insert the new line into</li>
  </ul>
  <p>Example</p>

  ```gherkin {4,6}
    Given the more field can be located by tag "textarea"
     When I navigate to "https://automationintesting.com/selenium/testpage"
      And I type "Hi," in the more field
      And I insert a new line in the more field
      And I type "I like Gwen." in the more field
      And I insert a new line in the more field
      And I type "Bye!" in the more field
     Then the more field should contain
          """
          Hi,
          I like Gwen.
          Bye!
          """
  ```

  <div class="grid-3">
    <div><Link to="#i-insert-a-new-line-in-textarea">Link</Link></div>
    <div align="center"></div>
    <div align="right">Since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.19.0">v2.19.0</Link></div>
  </div>
</details>

<details id="i-clear-element">
  <summary class="dsl">

  ```gherkin
  I clear <element>
  ```
  <p>Clears a text element.</p>
  </summary>
  <p>Where</p>
  <ul>
    <li><code>&lt;element&gt;</code> is the name of the element to clear</li>
  </ul>
  <p>Example</p>

  ```gherkin {3}
    Given the surname field can be located by id "surname"
     When I navigate to "https://automationintesting.com/selenium/testpage"
      And I clear the surname field
  ```

  <div class="grid-3">
    <div><Link to="#i-clear-element">Link</Link></div>
    <div align="center"></div>
    <div align="right">Since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v1.0.0">v1.0.0</Link></div>
  </div>
</details>

<details id="i-select-or-deselect-the-position-option-in-dropdown">
  <summary class="dsl">

  ```gherkin
  I <select|deselect> the <position><st|nd|rd|th> option in <dropdown>
  ```
  <p>Selects an option in a dropdown by position.</p>
  </summary>
  <p>Where</p>
  <ul>
    <li><code>&lt;select|deselect&gt;</code> is one of:
      <ul>
        <li><code>select</code> to select the option</li>
        <li><code>deselect</code> to deselect the option, since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.13.0">v2.13.0</Link>
          <ul>
            <li><i>Note</i>: Deselect will only work for multi-select dropdowns.</li>
          </ul>
        </li>
      </ul>
    </li>
    <li><code>&lt;position&gt;</code> is the position number of the option(1 for first position)</li>
    <li><code>&lt;st|nd|rd|th&gt;</code> is one of:
      <ul>
        <li><code>st</code> if the position number ends in 1</li>
        <li><code>nd</code> if the position number ends in 2</li>
        <li><code>rd</code> if the position number ends in 3</li>
        <li><code>th</code> if the position number is greater than 3</li>
      </ul>
    </li>
    <li><code>&lt;dropdown&gt;</code> is the name of the dropdown element</li>
  </ul>
  <p>Example</p>

  ```gherkin {3}
    Given the age dropdown can be located by id "challenger_age"
     When I navigate to "https://challengers.flood.io/step/2"
      And I select the 2nd option in the age dropdown
  ```

  <div class="grid-3">
    <div><Link to="#i-select-or-deselect-the-position-option-in-dropdown">Link</Link></div>
    <div align="center"></div>
    <div align="right">Since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v1.0.0">v1.0.0</Link></div>
  </div>
</details>

<details id="i-select-or-deselect-option-in-dropdown">
  <summary class="dsl">

  ```gherkin
  I <select|deselect> "<option>" in <dropdown>
  ```
  <p>Selects an option in a dropdown by displayed text.</p>
  </summary>
  <p>Where</p>
  <ul>
    <li><code>&lt;select|deselect&gt;</code> is one of:
      <ul>
        <li><code>select</code> to select the option</li>
        <li><code>deselect</code> to deselect the option, since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.13.0">v2.13.0</Link>
          <ul>
            <li><i>Note</i>: Deselect will only work for multi-select dropdowns.</li>
          </ul>
        </li>
      </ul>
    </li>
    <li><code>&lt;option&gt;</code> is the displayed option text</li>
    <li><code>&lt;dropdown&gt;</code> is the name of the dropdown element</li>
  </ul>
  <p>Example</p>

  ```gherkin {3}
    Given the age dropdown can be located by id "challenger_age"
     When I navigate to "https://challengers.flood.io/step/2"
      And I select "18" in the age dropdown
  ```

  <div class="grid-3">
    <div><Link to="#i-select-or-deselect-option-in-dropdown">Link</Link></div>
    <div align="center"></div>
    <div align="right">Since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v1.0.0">v1.0.0</Link></div>
  </div>
</details>

<details id="i-select-or-deselect-option-ref-in-dropdown">
  <summary class="dsl">

  ```gherkin
  I <select|deselect> <optionRef> in <dropdown>
  ```
  <p>Selects an option in a dropdown by a displayed text reference.</p>
  </summary>
  <p>Where</p>
  <ul>
    <li><code>&lt;select|deselect&gt;</code> is one of:
      <ul>
        <li><code>select</code> to select the option</li>
        <li><code>deselect</code> to deselect the option, since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.13.0">v2.13.0</Link>
          <ul>
            <li><i>Note</i>: Deselect will only work for multi-select dropdowns.</li>
          </ul>
        </li>
      </ul>
    </li>
    <li><code>&lt;optionRef&gt;</code> is the name of the binding containing the displayed option text
      <ul>
        <li>Can be the name of any binding that contains or resolves to a text value, including a web element in which case the text in the element will be dynamically retrieved and used.</li>
      </ul>
    </li>
    <li><code>&lt;dropdown&gt;</code> is the name of the dropdown element</li>
  </ul>
  <p>Example</p>

  ```gherkin {4}
    Given the age dropdown can be located by id "challenger_age"
      And my age is "24"
     When I navigate to "https://challengers.flood.io/step/2"
      And I select my age in the age dropdown
  ```

  <div class="grid-3">
    <div><Link to="#i-select-or-deselect-option-ref-in-dropdown">Link</Link></div>
    <div align="center"></div>
    <div align="right">Since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v1.0.0">v1.0.0</Link></div>
  </div>
</details>

<details id="i-select-or-deselect-option-in-dropdown-by-value">
  <summary class="dsl">

  ```gherkin
  I <select|deselect> "<option>" in <dropdown> by value
  ```
  <p>Selects an option in a dropdown by hidden value.</p>
  </summary>
  <p>Where</p>
  <ul>
    <li><code>&lt;select|deselect&gt;</code> is one of:
      <ul>
        <li><code>select</code> to select the option</li>
        <li><code>deselect</code> to deselect the option, since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.13.0">v2.13.0</Link>
          <ul>
            <li><i>Note</i>: Deselect will only work for multi-select dropdowns.</li>
          </ul>
        </li>
      </ul>
    </li>
    <li><code>&lt;option&gt;</code> is the hidden option value</li>
    <li><code>&lt;dropdown&gt;</code> is the name of the dropdown element</li>
  </ul>
  <p>Example</p>

  ```gherkin {3}
    Given the age dropdown can be located by id "challenger_age"
     When I navigate to "https://challengers.flood.io/step/2"
      And I select "21" in the age dropdown by value
  ```

  <div class="grid-3">
    <div><Link to="#i-select-or-deselect-option-in-dropdown-by-value">Link</Link></div>
    <div align="center"></div>
    <div align="right">Since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v1.0.0">v1.0.0</Link></div>
  </div>
</details>

<details id="i-select-or-deselect-option-ref-in-dropdown-by-value">
  <summary class="dsl">

  ```gherkin
  I <select|deselect> <optionRef> in <dropdown> by value
  ```
  <p>Selects an option in a dropdown by hidden value reference.</p>
  </summary>
  <p>Where</p>
  <ul>
    <li><code>&lt;select|deselect&gt;</code> is one of:
      <ul>
        <li><code>select</code> to select the option</li>
        <li><code>deselect</code> to deselect the option, since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.13.0">v2.13.0</Link>
          <ul>
            <li><i>Note</i>: Deselect will only work for multi-select dropdowns.</li>
          </ul>
        </li>
      </ul>
    </li>
    <li><code>&lt;optionRef&gt;</code> is the name of the binding containing the hidden option value
      <ul>
        <li>Can be the name of any binding that contains or resolves to a text value, including a web element in which case the text in the element will be dynamically retrieved and used.</li>
      </ul>
    </li>
    <li><code>&lt;dropdown&gt;</code> is the name of the dropdown element</li>
  </ul>
  <p>Example</p>

  ```gherkin {4}
    Given the age dropdown can be located by id "challenger_age"
      And the age is "24"
     When I navigate to "https://challengers.flood.io/step/2"
      And I deselect the age in the age dropdown
  ```

  <div class="grid-3">
    <div><Link to="#i-select-or-deselect-option-ref-in-dropdown-by-value">Link</Link></div>
    <div align="center"></div>
    <div align="right">Since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v1.0.0">v1.0.0</Link></div>
  </div>
</details>

<details id="i-scroll-to-the-top-or-bottom-of-element">
  <summary class="dsl">

  ```gherkin
  I scroll to the <top|bottom> of <element>
  ```
  <p>Scrolls to the top or bottom of the given element.</p>
  </summary>
  <p>Where</p>
  <ul>
    <li><code>&lt;top|bottom&gt;</code> is one of:
      <ul>
        <li><code>top</code> to scroll to the top of the element</li>
        <li><code>bottom</code> to scroll to the bottom of the element</li>
      </ul>
    </li>
    <li><code>&lt;element&gt;</code> the name of the element to scroll to</li>
  </ul>
  <p>Example</p>

  ```gherkin {3}
    Given the surname field can be located by id "surname"
     When I navigate to "https://automationintesting.com/selenium/testpage"
      And I scroll to the top of the surname field
  ```

  <div class="grid-3">
    <div><Link to="#i-scroll-to-the-top-or-bottom-of-element">Link</Link></div>
    <div align="center"></div>
    <div align="right">Since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v1.0.0">v1.0.0</Link></div>
  </div>
</details>

<details id="i-drag-and-drop-source-element-to-target-element">
  <summary class="dsl">

  ```gherkin
  I drag and drop <sourceElement> to <targetElement>
  ```
  <p>Drags and drops a source element onto a target element.</p>
  </summary>
  <p>Where</p>
  <ul>
    <li><code>&lt;sourceElement&gt;</code> is the name of the source element to drag</li>
    <li><code>&lt;targetElement&gt;</code> is the name of the target element to drop the <code>&lt;sourceElement&gt;</code> into</li>
  </ul>
  <p>Example</p>

  ```gherkin {5}
    Given the box can be located by xpath "//*[@id='credit2']/a"
      And the cell can be located by xpath "//*[@id='bank']/li"
	   When I navigate to "http://demo.guru99.com/test/drag_drop.html"
      And I capture the box as the box text
      And I drag and drop the box to the cell
     Then the cell should contain "${the box text}"
  ```

  <div class="grid-3">
    <div><Link to="#i-drag-and-drop-source-element-to-target-element">Link</Link></div>
    <div align="center"></div>
    <div align="right">Since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.17.0">v2.17.0</Link></div>
  </div>
</details>

<details id="i-wait-for-element">
  <summary class="dsl">

  ```gherkin
  I wait for <element>
  ```
  <p>Waits for an element to appear on a page for a default number of seconds before timing out.</p>
  </summary>
  <p>Where</p>
  <ul>
    <li><code>&lt;element&gt;</code> is the name of the element to wait for</li>
  </ul>
  <p>Example</p>

  ```gherkin {3}
    # Wait for a dynamically rendered input field 
    Given the dynamic input field can be located by tag "input"
      And I wait for the dynamic input field
  ```

  <div class="grid-3">
    <div><Link to="#i-wait-for-element">Link</Link></div>
    <div align="center"></div>
    <div align="right">Since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v1.0.0">v1.0.0</Link></div>
  </div>
</details>

<details id="i-wait-for-element-for-duration-seconds">
  <summary class="dsl">

  ```gherkin
  I wait for <element> for <duration> second[s]
  ```
  <p>Waits for an element to appear on a page for a given number of seconds before timing out.</p>
  </summary>
  <p>Where</p>
  <ul>
    <li><code>&lt;element&gt;</code> is the name of the element to wait for</li>
    <li><code>&lt;duration&gt;</code> is the number of seconds to wait before timing out</li>
    <li>Include trailing <code>s</code> when specifying a <code>&lt;duration&gt;</code> that is not 1, omit otherwise</li>
  </ul>
  <p>Example</p>

  ```gherkin {3}
    # Wait for a dynamically rendered input field 
    Given the dynamic input field can be located by tag "input"
      And I wait for the dynamic input field for 3 seconds
  ```

  <div class="grid-3">
    <div><Link to="#i-wait-for-element-for-duration-seconds">Link</Link></div>
    <div align="center"></div>
    <div align="right">Since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v1.0.0">v1.0.0</Link></div>
  </div>
</details>

<details id="i-wait-for-element-text">
  <summary class="dsl">

  ```gherkin
  I wait for <element> text
  ```
  <p>Waits for the text of an element to appear on a page for a default number of seconds before timing out.</p>
  </summary>
  <p>Where</p>
  <ul>
    <li><code>&lt;element&gt;</code> is the name of the element to wait for the text to appear</li>
  </ul>
  <p>Example</p>

  ```gherkin {3}
    # Wait for a dynamically displayed token
    Given the one time token can be located by javascript "$('.token').get(0)"
      And I wait for the one time token text
  ```

  <div class="grid-3">
    <div><Link to="#i-wait-for-element-text">Link</Link></div>
    <div align="center"></div>
    <div align="right">Since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v1.0.0">v1.0.0</Link></div>
  </div>
</details>

<details id="i-wait-for-element-text-for-duration-seconds">
  <summary class="dsl">

  ```gherkin
  I wait for <element> text for <duration> second[s]
  ```
  <p>Waits for the text of an element to appear on a page for a given number of seconds before timing out.</p>
  </summary>
  <p>Where</p>
  <ul>
    <li><code>&lt;element&gt;</code> is the name of the element to wait for the text to appear</li>
    <li><code>&lt;duration&gt;</code> is the number of seconds to wait before timing out</li>
    <li>Include trailing <code>s</code> when specifying a <code>&lt;duration&gt;</code> that is not 1, omit otherwise</li>
  </ul>
  <p>Example</p>

  ```gherkin {3}
    # Wait for a dynamically displayed token
    Given the one time token can be located by javascript "$('.token').get(0)"
      And I wait for the one time token text for 3 seconds
  ```

  <div class="grid-3">
    <div><Link to="#i-wait-for-element-text-for-duration-seconds">Link</Link></div>
    <div align="center"></div>
    <div align="right">Since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v1.0.0">v1.0.0</Link></div>
  </div>
</details>

<details id="i-wait-duration-seconds-when-element-is-actioned">
  <summary class="dsl">

  ```gherkin
  I wait <duration> second[s] when <element> is <actioned>
  ```
  <p>Triggers a sleep for a given number of seconds whenever a specific action is performed on an element.</p>
  </summary>
  <p>Where</p>
  <ul>
    <li><code>&lt;duration&gt;</code> is the number of seconds to sleep</li>
    <li><code>&lt;element&gt;</code> the name of the web element</li>
    <li><code>&lt;actioned&gt;</code> is one of:
      <ul>
        <li><code>clicked</code> to sleep when the element is clicked</li>
        <li><code>right clicked</code> to sleep when the element is right clicked, since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.8.0">v2.8.0</Link></li>
        <li><code>double clicked</code> to sleep when the element is double clicked, since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.12.0">v2.12.0</Link></li>
        <li><code>submitted</code> to sleep when the form element is submitted</li>
        <li><code>checked</code> (or <code>ticked</code> since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.4.0">v2.4.0</Link>) to sleep when the checkbox element is ticked</li>
        <li><code>unchecked</code> (or <code>unticked</code> since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.4.0">v2.4.0</Link>) to sleep when the checkbox element is unticked</li>
        <li><code>selected</code> to sleep when a dropdown option is selected in the element</li>
        <li><code>deselected</code> to sleep when a mutli-select dropdown option is deselected in the element, since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.13.0">v2.13.0</Link></li>
        <li><code>typed</code> to sleep when text is typed into the element</li>
        <li><code>entered</code> to sleep when text is entered into the element</li>
        <li><code>cleared</code> to sleep when the text is cleared in the element</li>
        <li><code>moved to</code> to sleep when the focus is moved to the element</li>
      </ul>
    </li>
    <li>Include <code>s</code> after <code>second</code> when specifying a <code>&lt;duration&gt;</code> that is not 1, omit otherwise</li>
  </ul>
  <p>Example</p>

  ```gherkin {2}
  Given the submit button can be located by id "submit"
    And I wait 3 seconds when the submit button is clicked
   When I navigate to "${token.website}"
    And I click the submit button
  ```

  <div class="grid-3">
    <div><Link to="#i-wait-duration-seconds-when-element-is-actioned">Link</Link></div>
    <div align="center"></div>
    <div align="right">Since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v1.0.0">v1.0.0</Link></div>
  </div>
</details>

<details id="i-wait-until-element-is-state">
  <summary class="dsl">

  ```gherkin
  I wait until <element> is[ not] <state>
  ```
  <p>Waits for an element to transition to a given state.</p>
  </summary>
  <p>Where</p>
  <ul>
    <li><code>&lt;element&gt;</code> is the name of the element to wait for</li>
    <li><code>not</code> negates the state if included</li>
    <li><code>&lt;state&gt;</code> is one of:
      <ul>
        <li><code>displayed</code> to wait until the element is displayed</li>
        <li><code>hidden</code> to wait until the element is not displayed</li>
        <li><code>checked</code> (or <code>ticked</code> since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.4.0">v2.4.0</Link>) to wait until a checkbox is ticked</li>
        <li><code>unchecked</code> (or <code>unticked</code> since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.4.0">v2.4.0</Link>) to wait until a checkbox is not ticked</li>
        <li><code>enabled</code> to wait until the element is enabled</li>
        <li><code>disabled</code> to wait until the element is disabled</li>
      </ul>
    </li>
  </ul>
  <p>Example</p>

  ```gherkin {5}
    Given my todo list can be located by css ".todo-list"
      And the todo field can be located by class "new-todo"
     When I navigate to "https://todomvc.com/examples/react"
      And I enter "Get the milk" in the todo field
      And I wait until my todo list is displayed
  ```

  <div class="grid-3">
    <div><Link to="#i-wait-until-element-is-state">Link</Link></div>
    <div align="center"></div>
    <div align="right">Since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.25.0">v2.25.0</Link></div>
  </div>
</details>

<details id="i-wait-until-condition-when-element-is-actioned">
  <summary class="dsl">

  ```gherkin
  I wait until <condition> when <element> is <actioned>
  ```
  <p>Triggers a wait for a condition whenever a specific action is performed on an element.</p>
  </summary>
  <p>Where</p>
  <ul>
    <li><code>&lt;condition&gt;</code> the name of a javascript binding containing a function that will return true when the condition is satisfied and false when it is not</li>
    <li><code>&lt;element&gt;</code> the name of the web element</li>
    <li><code>&lt;actioned&gt;</code> is one of:
      <ul>
        <li><code>clicked</code> to wait when the element is clicked</li>
        <li><code>right clicked</code> to wait when the element is right clicked, since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.8.0">v2.8.0</Link></li>
        <li><code>double clicked</code> to wait when the element is double clicked, since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.12.0">v2.12.0</Link></li>
        <li><code>submitted</code> to wait when the form element is submitted</li>
        <li><code>checked</code> (or <code>ticked</code> since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.4.0">v2.4.0</Link>) to wait when the checkbox element is ticked</li>
        <li><code>unchecked</code> (or <code>unticked</code> since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.4.0">v2.4.0</Link>) to wait when the checkbox element is unticked</li>
        <li><code>selected</code> to wait when a dropdown option is selected in the element</li>
        <li><code>deselected</code> to wait when a mutli-select dropdown option is deselected in the element, since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.13.0">v2.13.0</Link></li>
        <li><code>typed</code> to wait when text is typed into the element</li>
        <li><code>entered</code> to wait when text is entered into the element</li>
        <li><code>cleared</code> to wait when the text is cleared in the element</li>
        <li><code>moved to</code> to wait when the focus is moved to the element</li>
      </ul>
    </li>
    <li>Include <code>s</code> after <code>second</code> when specifying a <code>&lt;duration&gt;</code> that is not 1, omit otherwise</li>
  </ul>
  <p>Example</p>

  ```gherkin {3}
    Given the submit button can be located by name "submit"
      And the token field can be located by class "token"
      And the submit button is displayed is defined by javascript "!!document.getElementById('submit')"
      And I wait until the submit button is displayed when the token field is entered
     When I navigate to "${token.website}"
      And I enter "my token" in the token field
      And I click the submit button
  ```

  <div class="grid-3">
    <div><Link to="#i-wait-until-condition-when-element-is-actioned">Link</Link></div>
    <div align="center"></div>
    <div align="right">Since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v1.0.0">v1.0.0</Link></div>
  </div>
</details>

<details id="i-capture-element">
  <summary class="dsl">

  ```gherkin
  I capture <element>
  ```
  <p>Captures the text value of an element and binds it to the element name in the global scode.</p>
  </summary>
  <p>Where</p>
  <ul>
    <li><code>&lt;element&gt;</code> is the name of the element to capture the text of</li>
  </ul>
  <p>Example</p>

  ```gherkin {3}
    Given the heading can be located by tag "h1"
     When I navigate to "https://todomvc.com/examples/react"
      And I capture the heading
  ```

  <div class="grid-3">
    <div><Link to="#i-capture-element">Link</Link></div>
    <div align="center"></div>
    <div align="right">Since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v1.0.0">v1.0.0</Link></div>
  </div>
</details>

<details id="i-capture-element-as-name">
  <summary class="dsl">

  ```gherkin
  I capture <element> as <name>
  ```
  <p>Captures the text value of an element and binds it to the given name in the global scope.</p>
  </summary>
  <p>Where</p>
  <ul>
    <li><code>&lt;element&gt;</code> is the name of the element to capture the text of</li>
    <li><code>&lt;name&gt;</code> is the name to bind the captured text to</li>
  </ul>
  <p>Example</p>

  ```gherkin {3}
    Given the heading can be located by tag "h1"
     When I navigate to "https://todomvc.com/examples/react"
      And I capture the heading as my heading
  ```

  <div class="grid-3">
    <div><Link to="#i-capture-element-as-name">Link</Link></div>
    <div align="center"></div>
    <div align="right">Since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v1.0.0">v1.0.0</Link></div>
  </div>
</details>

<details id="i-capture-dropdown">
  <summary class="dsl">

  ```gherkin
  I capture <dropdown> <text|value>
  ```
  <p>Captures the selected text or value of a dropdown element and binds it to the element name in the global scode.</p>
  </summary>
  <p>Where</p>
  <ul>
    <li><code>&lt;dropdown&gt;</code> is the name of the dropdown element to capture the text or value of</li>
    <li><code>&lt;dropdown&gt;</code> is one of:
      <ul>
        <li><code>text</code> to captured the displayed text of the selection</li>
        <li><code>value</code> to captured the option value of the selection</li>
      </ul>
    </li>
  </ul>
  <p>Example</p>

  ```gherkin {3}
    Given the age dropdown can be located by id "challenger_age"
     When I navigate to "https://challengers.flood.io/step/2"
      And I capture the age dropdown text
  ```

  <div class="grid-3">
    <div><Link to="#i-capture-dropdown">Link</Link></div>
    <div align="center"></div>
    <div align="right">Since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v1.0.0">v1.0.0</Link></div>
  </div>
</details>

<details id="i-capture-dropdown-as-name">
  <summary class="dsl">

  ```gherkin
  I capture <dropdown> <text|value> as <name>
  ```
  <p>Captures the selected text or value of a dropdown element and binds it to the given name in the global scope.</p>
  </summary>
  <p>Where</p>
  <ul>
    <li><code>&lt;dropdown&gt;</code> is the name of the dropdown element to capture the text or value of</li>
    <li><code>&lt;dropdown&gt;</code> is one of:
      <ul>
        <li><code>text</code> to captured the displayed text of the selection</li>
        <li><code>value</code> to captured the option value of the selection</li>
      </ul>
    </li>
  </ul>
  <p>Example</p>

  ```gherkin {3}
    Given the age dropdown can be located by id "challenger_age"
     When I navigate to "https://challengers.flood.io/step/2"
      And I capture the age dropdown text my age
  ```

  <div class="grid-3">
    <div><Link to="#i-capture-dropdown-as-name">Link</Link></div>
    <div align="center"></div>
    <div align="right">Since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v1.0.0">v1.0.0</Link></div>
  </div>
</details>

<details id="i-capture-the-text-in-element-by-regex-expression-as-name">
  <summary class="dsl">

  ```gherkin
  I capture the text in <element> by regex "<expression>" as <name>
  ```
  <p>Catures text content from a web element using the given regex expression and binds it to the given name in the global scope.</p>
  </summary>
  <p>Where</p>
  <ul>
    <li><code>&lt;element&gt;</code> is the name of the element to capture text from</li>
    <li><code>&lt;expression&gt;</code> is the regex selector expression</li>
    <li><code>&lt;name&gt;</code> is the name to bind the captured value to</li>
  </ul>
  <p>Example</p>

  ```gherkin {3}
    # assuming a web element contains date: 2021-09-22
    Given the date field can be located by class "date"
     When I capture the text in the date field by regex "(\d{4})" as the year
     Then the year should be "2021"
  ```

  <div class="grid-3">
    <div><Link to="#i-capture-the-text-in-element-by-regex-expression-as-name">Link</Link></div>
    <div align="center"></div>
    <div align="right">Since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v1.0.0">v1.0.0</Link></div>
  </div>
</details>

<details id="i-capture-the-node-type-in-element-by-xpath-expression-as-name">
  <summary class="dsl">

  ```gherkin
  I capture the <nodeType> in <element> by xpath "<expression>" as <name>
  ```
  <p>Captures XML content from a web element using the given XPath expression and binds it to the given name in the global scope.</p>
  </summary>
  <p>Where</p>
  <ul>
    <li><code>&lt;nodeType&gt;</code> is one of:
      <ul>
        <li><code>text</code> to capture the text node</li>
        <li><code>node</code> to capture a single node</li>
        <li><code>nodeset</code> to capture multiple nodes</li>
      </ul>
    </li>
    <li><code>&lt;element&gt;</code> is the name of the element containing the XML content</li>
    <li><code>&lt;expression&gt;</code> is the XPath selector expression</li>
    <li><code>&lt;name&gt;</code> is the name to bind the captured value to</li>
  </ul>
  <p>Example</p>

  ```gherkin {3}
    # assuming a web element contains xml: <result><status>passed</status></result>
    Given the xml web element can be located by id "xml"
     When I capture the text in the xml web element by xpath "//result/status" as the status
     Then the status should be "passed"
  ```

  <div class="grid-3">
    <div><Link to="#i-capture-the-node-type-in-element-by-xpath-expression-as-name">Link</Link></div>
    <div align="center"></div>
    <div align="right">Since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v1.0.0">v1.0.0</Link></div>
  </div>
</details>

<details id="i-capture-the-content-in-element-by-json-path-expression-as-name">
  <summary class="dsl">

  ```gherkin
  I capture the content in <element> by json path "<expression>" as <name>
  ```
  <p>Captures JSON content from a web element using the given JSON Path expression and binds it to the given name in the global scope.</p>
  </summary>
  <p>Where</p>
  <ul>
    <li><code>&lt;element&gt;</code> is the name of the element containing the JSON content</li>
    <li><code>&lt;expression&gt;</code> is the <Link to="https://github.com/json-path/JsonPath/blob/master/README.md">JSON selector</Link> expression</li>
    <li><code>&lt;name&gt;</code> is the name to bind the captured value to</li>
  </ul>
  <p>Example</p>

  ```gherkin {3}
    # assuming a web element contains json: { "status": "passed" }
    Given the json web element can be located by id "json"
     When I capture the content in the json web element by json path "$.status" as the status
     Then the status should be "passed"
  ```

  <div class="grid-3">
    <div><Link to="#i-capture-the-content-in-element-by-json-path-expression-as-name">Link</Link></div>
    <div align="center"></div>
    <div align="right">Since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v1.4.0">v1.4.0</Link></div>
  </div>
</details>

<details id="i-capture-name-of-element-by-javascript-expression">
  <summary class="dsl">

  ```gherkin
  I capture <name> <of|on|in> <element> by <javascript|js> "<javascript>"
  ```
  <p>Executes a JavaScript expression or anonymous function on an element and binds the result to the given name.</p>
  </summary>
  <p>Where</p>
  <ul>
    <li><code>&lt;name&gt;</code> is the name to bind the javascript return value to</li>
    <li><code>&lt;of|on|in&gt;</code> is <code>of</code>, <code>on</code> or <code>in</code></li>
    <li><code>&lt;element&gt;</code> is the name of the element to execute the javascript on</li>
    <li><code>&lt;javascript|js&gt;</code> is <code>javascript</code> (or <code>js</code>, since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.46.0">v2.46.0</Link>)</li>
    <li><code>&lt;javascript&gt;</code> is the JavaScript expression or anonymous function to execute (can be in <Link to="https://github.com/gwen-interpreter/gwen/wiki/Doc-Strings">DocString</Link> position)
      <ul>
        <li>The reference to the element will be provided to the javascript through an implicit object named <code>element</code>.</li>
        <li>For an expression: no <code>return</code> prefix is required</li>
        <li>For an anonymous function: the syntax is <code>(function()&#123; body &#125;)()</code> where body must return a value</li>
      </ul>
    </li>
  </ul>
  <p>Example</p>

  ```gherkin {4-11}
    Given the red checkbox can be located by id "red"
     When I navigate to "https://automationintesting.com/selenium/testpage"
      And I click the red checkbox
      And I capture my favorite color in the red checkbox by js
          """
          (function() {
            var color = element.value
            if (!element.checked) color = "Blue"
            return color;
          })()
          """
     Then my favorite color should be "Red"
  ```

  <div class="grid-3">
    <div><Link to="#i-capture-name-of-element-by-javascript-expression">Link</Link></div>
    <div align="center"></div>
    <div align="right">Since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.12.0">v2.12.0</Link></div>
  </div>
</details>

<details id="i-base64-decode-element">
  <summary class="dsl">

  ```gherkin
  I base64 decode <element>
  ```
  <p>Base64 decodes the text in the given element and binds the result to the element name in the global scope.</p>
  </summary>
  <p>Where</p>
  <ul>
    <li><code>&lt;element&gt;</code> is the name of the element containing the text to decode</li>
  </ul>
  <p>Example</p>

  ```gherkin {2}
    Given the encoded field can be located by class "encoded"
     When I base64 decode the encoded field
  ```

  <div class="grid-3">
    <div><Link to="#i-base64-decode-element">Link</Link></div>
    <div align="center"></div>
    <div align="right">Since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v1.0.0">v1.0.0</Link></div>
  </div>
</details>

<details id="i-base64-decode-element-as-name">
  <summary class="dsl">

  ```gherkin
  I base64 decode <element> as <name>
  ```
  <p>Base64 decodes the text in the given element and binds the result to the give name in the global scope.</p>
  </summary>
  <p>Where</p>
  <ul>
    <li><code>&lt;element&gt;</code> is the name of the element containing the text to decode</li>
    <li><code>&lt;name&gt;</code> is the name to bind the decoded value to</li>
  </ul>
  <p>Example</p>

  ```gherkin {2}
    Given the encoded field can be located by class "encoded"
     When I base64 decode the encoded field as decoded value
  ```

  <div class="grid-3">
    <div><Link to="#i-base64-decode-element-as-name">Link</Link></div>
    <div align="center"></div>
    <div align="right">Since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v1.0.0">v1.0.0</Link></div>
  </div>
</details>

<details id="i-highlight-or-locate-element">
  <summary class="dsl">

  ```gherkin
  I <highlight|locate> <element>
  ```
  <p>Locates and highlights the given element on the current page.</p>
  </summary>
  <p>Where</p>
  <ul>
    <li><code>&lt;highlight|locate&gt;</code> is one of:
      <ul>
        <li><code>highlight</code> or <code>locate</code></li>
      </ul>
    </li>
    <li><code>&lt;element&gt;</code> is the name of the element</li>
  </ul>
  <p>Example</p>

  ```gherkin {3}
    Given the todo field can be located by class "new-todo"
     When I navigate to "https://todomvc.com/examples/react"
      And I locate the todo field
  ```

  <div class="grid-3">
    <div><Link to="#i-highlight-or-locate-element">Link</Link></div>
    <div align="center"></div>
    <div align="right">Since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v1.0.0">v1.0.0</Link></div>
  </div>
</details>

### Then

<details id="element-should-be-state">
  <summary class="dsl">

  ```gherkin
  <element> should[ not] be <state>
  ```
  <p>Checks whether or not an element is in a given state.</p>
  </summary>
  <p>Where</p>
  <ul>
    <li><code>&lt;element&gt;</code> is the name of the element to check</li>
    <li><code>not</code> negates the state if included</li>
    <li><code>&lt;state&gt;</code> is one of:
      <ul>
        <li><code>displayed</code> to check if the element is displayed</li>
        <li><code>hidden</code> to check if the element is not displayed</li>
        <li><code>checked</code> (or <code>ticked</code> since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.4.0">v2.4.0</Link>) to check if a checkbox is ticked</li>
        <li><code>unchecked</code> (or <code>unticked</code> since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.4.0">v2.4.0</Link>) to check if a checkbox is not ticked</li>
        <li><code>enabled</code> to check if the element is enabled</li>
        <li><code>disabled</code> to check if the element is disabled</li>
      </ul>
    </li>
  </ul>
  <p>Example</p>

  ```gherkin {3}
    Given my todo list can be located by css ".todo-list"
     When I navigate to "https://todomvc.com/examples/react"
     Then my todo list should be hidden
  ```

  <div class="grid-3">
    <div><Link to="#element-should-be-state">Link</Link></div>
    <div align="center"></div>
    <div align="right">Since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v1.0.0">v1.0.0</Link></div>
  </div>
</details>

<details id="element-should-match-expression">
  <summary class="dsl">

  ```gherkin
  <element> should[ not] <match> "<expression>"
  ```
  
  <p>Checks whether or not the text value of an element matches a given expression. Reports an assertion error if the match fails.</p>
  </summary>
  <p>Where</p>
  <ul>
    <li><code>&lt;element&gt;</code> is the name of the element to check</li>
    <li><code>not</code> negates the match if included</li>
    <li><code>&lt;match&gt;</code> is the type of match to perform, one of:
      <ul>
        <li><code>be</code> for exact match</li>
        <li><code>contain</code> for partial match</li>
        <li><code>start with</code> for partial leading match</li>
        <li><code>end with</code> for partial trailing match</li>
        <li><code>match regex</code> for regex match</li>
        <li><code>match xpath</code> for XML match</li>
        <li><code>match json path</code> for <Link to="https://github.com/json-path/JsonPath/blob/master/README.md">JSON match</Link>, since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v1.4.0">v1.4.0</Link></li>
        <li><code>match template</code> for <Link to="https://github.com/gwen-interpreter/gwen/wiki/Template-Matching">template</Link> match, since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.16.0">v2.16.0</Link></li>
        <li><code>match template file</code> for <Link to="https://github.com/gwen-interpreter/gwen/wiki/Template-Matching">template</Link> file match, since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.16.0">v2.16.0</Link></li>
      </ul>
    </li>
    <li><code>&lt;expression&gt;</code> is the expression to match (can be in <Link to="https://github.com/gwen-interpreter/gwen/wiki/Doc-Strings">DocString</Link> position)</li>
  </ul>
  <p>Example</p>

  ```gherkin {5}
    Given the todo field can be located by class "new-todo"
      And my todo list can be located by css ".todo-list"
     When I navigate to "https://todomvc.com/examples/react"
      And I enter "Get the milk" in the todo field
     Then my todo list should contain "Get the milk"
  ```
  
  <div class="grid-3">
    <div><Link to="#element-should-match-expression">Link</Link></div>
    <div align="center"></div>
    <div align="right">Since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v1.0.0">v1.0.0</Link></div>
  </div>
</details>

<details id="element-should-match-expression-ref">
  <summary class="dsl">

  ```gherkin
  <element> should[ not] <match> <expressionRef>
  ```
  
  <p>Checks whether or not the text value of an element matches a referenced expression. Reports an assertion error if the match fails.</p>
  </summary>
  <p>Where</p>
  <ul>
    <li><code>&lt;element&gt;</code> is the name of the element to check</li>
    <li><code>not</code> negates the match if included</li>
    <li><code>&lt;match&gt;</code> is the type of match to perform, one of:
      <ul>
        <li><code>be</code> for exact match</li>
        <li><code>contain</code> for partial match</li>
        <li><code>start with</code> for partial leading match</li>
        <li><code>end with</code> for partial trailing match</li>
        <li><code>match regex</code> for regex match</li>
        <li><code>match xpath</code> for XML match</li>
        <li><code>match json path</code> for <Link to="https://github.com/json-path/JsonPath/blob/master/README.md">JSON match</Link>, since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v1.4.0">v1.4.0</Link></li>
        <li><code>match template</code> for <Link to="https://github.com/gwen-interpreter/gwen/wiki/Template-Matching">template</Link> match, since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.16.0">v2.16.0</Link></li>
        <li><code>match template file</code> for <Link to="https://github.com/gwen-interpreter/gwen/wiki/Template-Matching">template</Link> file match, since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.16.0">v2.16.0</Link></li>
      </ul>
    </li>
    <li><code>&lt;expressionRef&gt;</code> is the name of the binding containing the expression to match
      <ul>
        <li>Can be the name of any binding that contains or resolves to an expression value, including a web element in which case the text in the element will be dynamically retrieved and used.</li>
      </ul>
    </li>
  </ul>
  <p>Example</p>

  ```gherkin {6}
    Given the todo field can be located by class "new-todo"
      And my todo list can be located by css ".todo-list"
      And my item is "Feed the cat"
     When I navigate to "https://todomvc.com/examples/react"
      And I enter my item in the todo field
     Then my todo list should contain my item
  ```
  
  <div class="grid-3">
    <div><Link to="#element-should-match-expression-ref">Link</Link></div>
    <div align="center"></div>
    <div align="right">Since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v1.0.0">v1.0.0</Link></div>
  </div>
</details>

<details id="drodown-text-or-value-should-match-expression">
  <summary class="dsl">

  ```gherkin
  <dropdown> <text|value> should[ not] <match> "<expression>"
  ```
  
  <p>Checks whether or not a dropdown selection matches a given expression. Reports an assertion error if the match fails.</p>
  </summary>
  <p>Where</p>
  <ul>
    <li><code>&lt;dropdown&gt;</code> is the dropdown element to check</li>
    <li><code>&lt;text|value&gt;</code> is one of:
      <ul>
        <li><code>text</code> to check the displayed text of the selection</li>
        <li><code>value</code> to check the option value of the selection</li>
      </ul>
    </li>
    <li><code>not</code> negates the match if included</li>
    <li><code>&lt;match&gt;</code> is the type of match to perform, one of:
      <ul>
        <li><code>be</code> for exact match</li>
        <li><code>contain</code> for partial match</li>
        <li><code>start with</code> for partial leading match</li>
        <li><code>end with</code> for partial trailing match</li>
        <li><code>match regex</code> for regex match</li>
        <li><code>match xpath</code> for XML match</li>
        <li><code>match json path</code> for <Link to="https://github.com/json-path/JsonPath/blob/master/README.md">JSON match</Link>, since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v1.4.0">v1.4.0</Link></li>
        <li><code>match template</code> for <Link to="https://github.com/gwen-interpreter/gwen/wiki/Template-Matching">template</Link> match, since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.16.0">v2.16.0</Link></li>
        <li><code>match template file</code> for <Link to="https://github.com/gwen-interpreter/gwen/wiki/Template-Matching">template</Link> file match, since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.16.0">v2.16.0</Link></li>
      </ul>
    </li>
    <li><code>&lt;expression&gt;</code> is the expression to match (can be in <Link to="https://github.com/gwen-interpreter/gwen/wiki/Doc-Strings">DocString</Link> position)</li>
  </ul>
  <p>Example</p>

  ```gherkin {4}
    Given the age dropdown can be located by id "challenger_age"
     When I navigate to "https://challengers.flood.io/step/2"
      And I select the 2nd option in the age dropdown
     Then the age dropdown value should be "18"
  ```
  
  <div class="grid-3">
    <div><Link to="#drodown-text-or-value-should-match-expression">Link</Link></div>
    <div align="center"></div>
    <div align="right">Since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v1.0.0">v1.0.0</Link></div>
  </div>
</details>

<details id="drodown-text-or-value-should-match-expression-ref">
  <summary class="dsl">

  ```gherkin
  <dropdown> <text|value> should[ not] <match> <expressionRef>
  ```
  
  <p>Checks whether or not a dropdown selection matches a referenced expression. Reports an assertion error if the match fails.</p>
  </summary>
  <p>Where</p>
  <ul>
    <li><code>&lt;dropdown&gt;</code> is the dropdown element to check</li>
    <li><code>&lt;text|value&gt;</code> is one of:
      <ul>
        <li><code>text</code> to check the displayed text of the selection</li>
        <li><code>value</code> to check the option value of the selection</li>
      </ul>
    </li>
    <li><code>not</code> negates the match if included</li>
    <li><code>&lt;match&gt;</code> is the type of match to perform, one of:
      <ul>
        <li><code>be</code> for exact match</li>
        <li><code>contain</code> for partial match</li>
        <li><code>start with</code> for partial leading match</li>
        <li><code>end with</code> for partial trailing match</li>
        <li><code>match regex</code> for regex match</li>
        <li><code>match xpath</code> for XML match</li>
        <li><code>match json path</code> for <Link to="https://github.com/json-path/JsonPath/blob/master/README.md">JSON match</Link>, since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v1.4.0">v1.4.0</Link></li>
        <li><code>match template</code> for <Link to="https://github.com/gwen-interpreter/gwen/wiki/Template-Matching">template</Link> match, since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.16.0">v2.16.0</Link></li>
        <li><code>match template file</code> for <Link to="https://github.com/gwen-interpreter/gwen/wiki/Template-Matching">template</Link> file match, since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.16.0">v2.16.0</Link></li>
      </ul>
    </li>
    <li><code>&lt;expressionRef&gt;</code> is the name of the binding containing the expression to match
      <ul>
        <li>Can be the name of any binding that contains or resolves to an expression value, including a web element in which case the text in the element will be dynamically retrieved and used.</li>
      </ul>
    </li>
  </ul>
  <p>Example</p>

  ```gherkin {5}
    Given the age dropdown can be located by id "challenger_age"
      And my age is "18"
     When I navigate to "https://challengers.flood.io/step/2"
      And I select the 2nd option in the age dropdown
     Then the age dropdown value should be my age
  ```
  
  <div class="grid-3">
    <div><Link to="#drodown-text-or-value-should-match-expression-ref">Link</Link></div>
    <div align="center"></div>
    <div align="right">Since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v1.0.0">v1.0.0</Link></div>
  </div>
</details>

## Browser windows and tabs

### Given

<details id="i-have-an-open-browser">
  <summary class="dsl">

  ```gherkin
  I have an open browser
  ```
  <p>Opens a browser if none are open</p>
  </summary>
  <p>Example</p>

  ```gherkin {1}
    Given I have an open browser
     When I navigate to "https://todomvc.com/examples/react"
     Then I should have 1 open browser
  ```

  <div class="grid-3">
    <div><Link to="#i-have-an-open-browser">Link</Link></div>
    <div align="center"></div>
    <div align="right">Since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.41.0">v2.41.0</Link></div>
  </div>
</details>

<details id="i-have-no-open-browser">
  <summary class="dsl">

  ```gherkin
  I have no open browser
  ```
  <p>Closes all open browsers</p>
  </summary>
  <p>Example</p>

  ```gherkin {1}
    Given I have no open browser
     When I navigate to "https://todomvc.com/examples/react"
     Then I should have 1 open browser
  ```

  <div class="grid-3">
    <div><Link to="#i-have-no-open-browser">Link</Link></div>
    <div align="center"></div>
    <div align="right">Since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.41.0">v2.41.0</Link></div>
  </div>
</details>

### When

<details id="i-start-a-new-browser">
  <summary class="dsl">

  ```gherkin
  I start a new browser
  ```
  <p>Starts a new browser session (or switches to exising one if it is already open).</p>
  </summary>
  <p>Example</p>

  ```gherkin {2}
   Given I have no open browser
    When I start a new browser
    Then I should have 1 open browser
  ```

  <div class="grid-3">
    <div><Link to="#i-start-a-new-browser">Link</Link></div>
    <div align="center"></div>
    <div align="right">Since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v1.2.0">v1.2.0</Link></div>
  </div>
</details>

<details id="i-close-the-current-browser">
  <summary class="dsl">

  ```gherkin
  I close the[ current] browser
  ```
  <p>Closes the current browser session (or does nothing if one is not open).</p>
  </summary>
  <p>Where</p>
  <ul>
    <li><code>[ current]</code> including the word <code>current</code> is optional and makes no difference, since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v1.3.0">v1.3.0</Link></li>
  </ul>
  <p>Example</p>

  ```gherkin {2,4}
    When I navigate to "https://todomvc.com/examples/react"
     And I close the current browser
     And I navigate to "https://todomvc.com/examples/vue/"
     And I close the browser
  ```

  <div class="grid-3">
    <div><Link to="#i-close-the-current-browser">Link</Link></div>
    <div align="center"></div>
    <div align="right">Since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v1.2.0">v1.2.0</Link></div>
  </div>
</details>

<details id="i-start-a-browser-for-name">
  <summary class="dsl">

  ```gherkin
  I start a browser for <name>
  ```
  <p>Starts a new browser session with the given name (closes any existing browser of that name first).</p>
  </summary>
  <p>Where</p>
  <ul>
    <li><code>&lt;name&gt;</code> is a name assigned to the browser session</li>
  </ul>
  <p>Example</p>

  ```gherkin {1,3}
    When I start a browser for React
     And I navigate to "https://todomvc.com/examples/react"
     And I start a browser for Vue
     And I navigate to "https://todomvc.com/examples/vue/"
    Then I should have 2 open browsers
  ```

  <div class="grid-3">
    <div><Link to="#i-start-a-browser-for-name">Link</Link></div>
    <div align="center"></div>
    <div align="right">Since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v1.2.0">v1.2.0</Link></div>
  </div>
</details>

<details id="i-switch-to-name">
  <summary class="dsl">

  ```gherkin
  I switch to <name>
  ```
  <p>Switches to a named browser session (starts a new browser if one of that name is not already open). All browser actions performed after this will execute in the named session.</p>
  </summary>
  <p>Where</p>
  <ul>
    <li><code>&lt;name&gt;</code> is the name of the browser session to switch to</li>
  </ul>
  <p>Example</p>

  ```gherkin {5}
    When I start a browser for React
     And I navigate to "https://todomvc.com/examples/react"
     And I start a browser for Vue
     And I navigate to "https://todomvc.com/examples/vue/"
     And I switch to React
    Then the current URL should contain "react"
  ```

  <div class="grid-3">
    <div><Link to="#i-switch-to-name">Link</Link></div>
    <div align="center"></div>
    <div align="right">Since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v1.2.0">v1.2.0</Link></div>
  </div>
</details>

<details id="i-close-the-browser-for-name">
  <summary class="dsl">

  ```gherkin
  I close the browser for <name>
  ```
  <p>Closes a named browser session (does nothing if one of that name is not open).</p>
  </summary>
  <p>Where</p>
  <ul>
    <li><code>&lt;name&gt;</code> is the name of the browser session to close</li>
  </ul>
  <p>Example</p>

  ```gherkin {3}
    When I start a browser for React
     And I navigate to "https://todomvc.com/examples/react"
     And I close the browser for React
  ```

  <div class="grid-3">
    <div><Link to="#i-close-the-browser-for-name">Link</Link></div>
    <div align="center"></div>
    <div align="right">Since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v1.2.0">v1.2.0</Link></div>
  </div>
</details>

<details id="i-switch-to-the-default-content">
  <summary class="dsl">

  ```gherkin
  I switch to the default content
  ```
  <p>Switches from an iframe to the default or main content window.</p>
  </summary>
  <p>Example</p>

  ```gherkin {2}
    When I navigate to "https://page-with-iframes"
     And I switch to the default content
  ```

  <div class="grid-3">
    <div><Link to="#i-switch-to-the-default-content">Link</Link></div>
    <div align="center"></div>
    <div align="right">Since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.16.0">v2.16.0</Link></div>
  </div>
</details>

<details id="i-switch-to-the-child-tab-or-window">
  <summary class="dsl">

  ```gherkin
  I switch to the child <tab|window>
  ```
  <p>Switches to the most recently opened child tab or window (without closing the parent window)</p>
  </summary>
  <p>Where</p>
  <ul>
    <li><code>&lt;tab|window&gt;</code> is either <code>tab</code> or <code>window</code></li>
  </ul>
  <p>Example</p>

  ```gherkin {4}
    Given the new window button can be located by name "newbrowserwindow123"
     When I navigate to "https://nxtgenaiacademy.com/multiplewindows/"
      And I click the new window button
      And I switch to the child window
  ```

  <div class="grid-3">
    <div><Link to="#i-switch-to-the-child-tab-or-window">Link</Link></div>
    <div align="center"></div>
    <div align="right">Since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v1.4.0">v1.4.0</Link></div>
  </div>
</details>

<details id="i-close-the-child-tab-or-window">
  <summary class="dsl">

  ```gherkin
  I close the child <tab|window>
  ```
  <p>Closes the currently active child tab or window and passes control back to the parent window.</p>
  </summary>
  <p>Where</p>
  <ul>
    <li><code>&lt;tab|window&gt;</code> is either <code>tab</code> or <code>window</code></li>
  </ul>
  <p>Example</p>

  ```gherkin {5}
    Given the new tab button can be located by name "newbrowsertab453"
     When I navigate to "https://nxtgenaiacademy.com/multiplewindows/"
      And I click the new tab button
      And I switch to the child tab
      And I close the child tab
  ```

  <div class="grid-3">
    <div><Link to="#i-close-the-child-tab-or-window">Link</Link></div>
    <div align="center"></div>
    <div align="right">Since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v1.4.0">v1.4.0</Link></div>
  </div>
</details>

<details id="i-switch-to-child-tab-or-window-occurrence">
  <summary class="dsl">

  ```gherkin
  I switch to child <tab|window> <occurrence>
  ```
  <p>Switches to a child tab or window occurrence (without closing the parent window).</p>
  </summary>
  <p>Where</p>
  <ul>
    <li><code>&lt;tab|window&gt;</code> is either <code>tab</code> or <code>window</code></li>
    <li><code>&lt;occurrence&gt;</code> is the tab or window occurrence to switch to (1st opened is occurrence 1, 2nd is 2, ..)</li>
  </ul>
  <p>Example</p>

  ```gherkin {4}
    Given the new window button can be located by name "newbrowserwindow123"
     When I navigate to "https://nxtgenaiacademy.com/multiplewindows/"
      And I click the new window button
      And I switch to child window 1
  ```

  <div class="grid-3">
    <div><Link to="#i-switch-to-child-tab-or-window-occurrence">Link</Link></div>
    <div align="center"></div>
    <div align="right">Since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.49.0">v2.49.0</Link></div>
  </div>
</details>

<details id="i-close-child-tab-or-window-occurrence">
  <summary class="dsl">

  ```gherkin
  I close child <tab|window> <occurrence>
  ```
  <p>Closes a child tab or window occurrence and passes control back to the parent window.</p>
  </summary>
  <p>Where</p>
  <ul>
    <li><code>&lt;tab|window&gt;</code> is either <code>tab</code> or <code>window</code></li>
    <li><code>&lt;occurrence&gt;</code> is the tab or window occurrence to close (1st opened is occurrence 1, 2nd is 2, ..)</li>
  </ul>
  <p>Example</p>

  ```gherkin {5}
    Given the new tab button can be located by name "newbrowsertab453"
     When I navigate to "https://nxtgenaiacademy.com/multiplewindows/"
      And I click the new tab button
      And I switch to child tab 1
      And I close child tab 1
  ```

  <div class="grid-3">
    <div><Link to="#i-close-child-tab-or-window-occurrence">Link</Link></div>
    <div align="center"></div>
    <div align="right">Since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.49.0">v2.49.0</Link></div>
  </div>
</details>

<details id="i-switch-to-the-parent-tab-or-window">
  <summary class="dsl">

  ```gherkin
  I switch to the <parent|root> <tab|window>
  ```
  <p>Switches to the parent window (without closing the currently active child window).</p>
  </summary>
  <p>Where</p>
  <ul>
    <li><code>&lt;parent|root&gt;</code> is either <code>parent</code> (or <code>root</code> since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.49.0">v2.49.0</Link>)</li>
    <li><code>&lt;tab|window&gt;</code> is either <code>tab</code> or <code>window</code></li>
  </ul>
  <p>Example</p>

  ```gherkin {5}
    Given the new window button can be located by name "newbrowserwindow123"
     When I navigate to "https://nxtgenaiacademy.com/multiplewindows/"
      And I click the new window button
      And I switch to child window 1
      And I switch to the parent window
  ```

  <div class="grid-3">
    <div><Link to="#i-switch-to-the-parent-tab-or-window">Link</Link></div>
    <div align="center"></div>
    <div align="right">Since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v1.4.0">v1.4.0</Link></div>
  </div>
</details>

<details id="i-maximize-the-window">
  <summary class="dsl">

  ```gherkin
  I <maximize|maximise> the window
  ```
  <p>Maximizes the currently active browser window to fill the entire screen area.</p>
  </summary>
  <p>Where</p>
  <ul>
    <li><code>&lt;maximize|maximise&gt;</code> is either <code>maximize</code> or <code>maximise</code> (makes no difference)</li>
  </ul>
  <p>Example</p>

  ```gherkin {2}
    When I navigate to "https://todomvc.com/examples/react"
     And I maximize the window
  ```

  <div class="grid-3">
    <div><Link to="#i-maximize-the-window">Link</Link></div>
    <div align="center"></div>
    <div align="right">Since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.2.0">v2.2.0</Link></div>
  </div>
</details>

<details id="i-resize-the-window-to-width-w-and-height-h">
  <summary class="dsl">

  ```gherkin
  I resize the window to width <w> and height <h>
  ```
  <p>Resizes the currently active browser window to the given dimensions.</p>
  </summary>
  <p>Where</p>
  <ul>
    <li><code>&lt;w&gt;</code> is the width in pixels</li>
    <li><code>&lt;h&gt;</code> is the height in pixels</li>
  </ul>
  <p>Example</p>

  ```gherkin {2}
    When I navigate to "https://todomvc.com/examples/react"
     And I resize the window to width 1280 and height 720
  ```

  <div class="grid-3">
    <div><Link to="#i-resize-the-window-to-width-w-and-height-h">Link</Link></div>
    <div align="center"></div>
    <div align="right">Since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.2.0">v2.2.0</Link></div>
  </div>
</details>

### Then

<details id="i-should-have-1-open-browser">
  <summary class="dsl">

  ```gherkin
  I should have 1 open browser
  ```
  <p>Checks that only one browser session is currently open. Reports an assertion error if not.</p>
  </summary>
  <p>Example</p>

  ```gherkin {3}
   Given I have no open browser
    When I navigate to "https://todomvc.com/examples/react"
    Then I should have 1 open browser
  ```

  <div class="grid-3">
    <div><Link to="#i-should-have-1-open-browser">Link</Link></div>
    <div align="center"></div>
    <div align="right">Since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.41.0">v2.41.0</Link></div>
  </div>
</details>

<details id="i-should-have-count-open-browsers">
  <summary class="dsl">

  ```gherkin
  I should have <count> open browsers
  ```
  <p>Checks that a given number of browsers are currently open. Reports an assertion error if not.</p>
  </summary>
  <p>Where</p>
  <ul>
    <li><code>count</code> the expected number of open browsers</li>
  </ul>
  <p>Example</p>

  ```gherkin {5}
   Given I have no open browser
    When I navigate to "https://todomvc.com/examples/react"
     And I start a browser for Vue
     And I navigate to "https://todomvc.com/examples/vue"
    Then I should have 2 open browsers
  ```

  <div class="grid-3">
    <div><Link to="#i-should-have-count-open-browsers">Link</Link></div>
    <div align="center"></div>
    <div align="right">Since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.41.0">v2.41.0</Link></div>
  </div>
</details>

## Alerts and popups

### When

<details id="i-accept-the-alert-or-confirmation-popup">
  <summary class="dsl">

  ```gherkin
  I accept the <alert|confirmation> popup
  ```
  <p>Accepts and closes an alert or confirmation popup by clicking OK.</p>
  </summary>
  <p>Where</p>
  <ul>
    <li><code>&lt;alert|confirmation&gt;</code> is either:
      <ul>
        <li><code>alert</code> for an alert box with an OK button</li>
        <li><code>confirmation</code> for a confirmation box with OK and Cancel buttons</li>
      </ul>
    </li>
  </ul>
  <p>Example</p>

  ```gherkin {4}
    Given the alert button can be located by css ".btn" at index 0
     When I navigate to "https://www.seleniumeasy.com/test/javascript-alert-box-demo.html"
      And I click the alert button
      And I accept the alert popup
  ```

  <div class="grid-3">
    <div><Link to="#i-accept-the-alert-or-confirmation-popup">Link</Link></div>
    <div align="center"></div>
    <div align="right">Since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v1.4.0">v1.4.0</Link></div>
  </div>
</details>

<details id="i-dismiss-the-confirmation-popup">
  <summary class="dsl">

  ```gherkin
  I dismiss the confirmation popup
  ```
  <p>Dismisses and closes a confirmation popup by clicking Cancel.</p>
  </summary>
  <p>Example</p>

  ```gherkin {4}
    Given the confirmation button can be located by css ".btn" at index 1
     When I navigate to "https://www.seleniumeasy.com/test/javascript-alert-box-demo.html"
      And I click the confirmation button
      And I dismiss the confirmation popup
  ```

  <div class="grid-3">
    <div><Link to="#i-dismiss-the-confirmation-popup">Link</Link></div>
    <div align="center"></div>
    <div align="right">Since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v1.4.0">v1.4.0</Link></div>
  </div>
</details>

<details id="i-capture-the-alert-or-confirmation-popup-message">
  <summary class="dsl">

  ```gherkin
  I capture the <alert|confirmation> popup message
  ```
  <p>Captures the alert or confirmation popup message and binds it to <code>the &lt;alert|confirmation&gt; popup message</code> in the global scope.</p>
  </summary>
  <p>Where</p>
  <ul>
    <li><code>&lt;alert|confirmation&gt;</code> is either:
      <ul>
        <li><code>alert</code> for an alert box with an OK button</li>
        <li><code>confirmation</code> for a confirmation box with OK and Cancel buttons</li>
      </ul>
    </li>
  </ul>
  <p>Example</p>

  ```gherkin {4}
    Given the alert button can be located by css ".btn" at index 0
     When I navigate to "https://www.seleniumeasy.com/test/javascript-alert-box-demo.html"
      And I click the alert button
      And I capture the alert popup message
  ```

  <div class="grid-3">
    <div><Link to="#i-capture-the-alert-or-confirmation-popup-message">Link</Link></div>
    <div align="center"></div>
    <div align="right">Since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.24.0">v2.24.0</Link></div>
  </div>
</details>

<details id="i-capture-the-alert-or-confirmation-popup-message-as-name">
  <summary class="dsl">

  ```gherkin
  I capture the <alert|confirmation> popup message as <name>
  ```
  <p>Captures the alert or confirmation popup message and binds it to the given name in the global scope.</p>
  </summary>
  <p>Where</p>
  <ul>
    <li><code>&lt;alert|confirmation&gt;</code> is either:
      <ul>
        <li><code>alert</code> for an alert box with an OK button</li>
        <li><code>confirmation</code> for a confirmation box with OK and Cancel buttons</li>
      </ul>
    </li>
  </ul>
  <p>Example</p>

  ```gherkin {4}
    Given the confirmation button can be located by css ".btn" at index 1
     When I navigate to "https://www.seleniumeasy.com/test/javascript-alert-box-demo.html"
      And I click the confirmation button
      And I capture the confirmation popup message as the alert message
  ```

  <div class="grid-3">
    <div><Link to="#i-capture-the-alert-or-confirmation-popup-message-as-name">Link</Link></div>
    <div align="center"></div>
    <div align="right">Since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.24.0">v2.24.0</Link></div>
  </div>
</details>

### Then

<details id="the-alert-or-confirmation-popup-message-should-match-expression">
  <summary class="dsl">

  ```gherkin
  the <alert|confirmation> popup message should[ not] <match> "<expression>"
  ```
  
  <p>Checks whether or not the message in the alert or confirmation popup box matches a given expression. Reports an assertion error if the match fails.</p>
  </summary>
  <p>Where</p>
  <ul>
    <li><code>not</code> negates the match if included</li>
    <li><code>&lt;match&gt;</code> is the type of match to perform, one of:
      <ul>
        <li><code>be</code> for exact match</li>
        <li><code>contain</code> for partial match</li>
        <li><code>start with</code> for partial leading match</li>
        <li><code>end with</code> for partial trailing match</li>
        <li><code>match regex</code> for regex match</li>
        <li><code>match xpath</code> for XML match</li>
        <li><code>match json path</code> for <Link to="https://github.com/json-path/JsonPath/blob/master/README.md">JSON match</Link>, since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v1.4.0">v1.4.0</Link></li>
        <li><code>match template</code> for <Link to="https://github.com/gwen-interpreter/gwen/wiki/Template-Matching">template</Link> match, since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.16.0">v2.16.0</Link></li>
        <li><code>match template file</code> for <Link to="https://github.com/gwen-interpreter/gwen/wiki/Template-Matching">template</Link> file match, since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.16.0">v2.16.0</Link></li>
      </ul>
    </li>
    <li><code>&lt;expression&gt;</code> is the expression to match (can be in <Link to="https://github.com/gwen-interpreter/gwen/wiki/Doc-Strings">DocString</Link> position)</li>
  </ul>
  <p>Example</p>

  ```gherkin {4}
    Given the alert button can be located by css ".btn" at index 0
     When I navigate to "https://www.seleniumeasy.com/test/javascript-alert-box-demo.html"
      And I click the alert button
     Then the alert popup message should be "I am an alert box!"
  ```
  
  <div class="grid-3">
    <div><Link to="#the-alert-or-confirmation-popup-message-should-match-expression">Link</Link></div>
    <div align="center"></div>
    <div align="right">Since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.24.0">v2.24.0</Link></div>
  </div>
</details>

<details id="the-alert-or-confirmation-popup-message-should-match-expression-ref">
  <summary class="dsl">

  ```gherkin
  the <alert|confirmation> popup message should[ not] <match> <expressionRef>
  ```
  
  <p>Checks whether or not the message in the alert or confirmation popup box matches a referenced expression. Reports an assertion error if the match fails.</p>
  </summary>
  <p>Where</p>
  <ul>
    <li><code>not</code> negates the match if included</li>
    <li><code>&lt;match&gt;</code> is the type of match to perform, one of:
      <ul>
        <li><code>be</code> for exact match</li>
        <li><code>contain</code> for partial match</li>
        <li><code>start with</code> for partial leading match</li>
        <li><code>end with</code> for partial trailing match</li>
        <li><code>match regex</code> for regex match</li>
        <li><code>match xpath</code> for XML match</li>
        <li><code>match json path</code> for <Link to="https://github.com/json-path/JsonPath/blob/master/README.md">JSON match</Link>, since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v1.4.0">v1.4.0</Link></li>
        <li><code>match template</code> for <Link to="https://github.com/gwen-interpreter/gwen/wiki/Template-Matching">template</Link> match, since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.16.0">v2.16.0</Link></li>
        <li><code>match template file</code> for <Link to="https://github.com/gwen-interpreter/gwen/wiki/Template-Matching">template</Link> file match, since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.16.0">v2.16.0</Link></li>
      </ul>
    </li>
    <li><code>&lt;expressionRef&gt;</code> is the name of the binding containing the expression to match
      <ul>
        <li>Can be the name of any binding that contains or resolves to an expression value, including a web element in which case the text in the element will be dynamically retrieved and used.</li>
      </ul>
    </li>
  </ul>
  <p>Example</p>

  ```gherkin {5}
    Given the confirmation button can be located by css ".btn" at index 1
      And the expected message is "Press a button!"
     When I navigate to "https://www.seleniumeasy.com/test/javascript-alert-box-demo.html"
      And I click the confirmation button
     Then the confirmation popup message should be the expected message
  ```
  
  <div class="grid-3">
    <div><Link to="#the-alert-or-confirmation-popup-message-should-match-expression-ref">Link</Link></div>
    <div align="center"></div>
    <div align="right">Since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.24.0">v2.24.0</Link></div>
  </div>
</details>

## Control Structures

### For Each

<details id="step-for-each-element-located-by-selector-expression">
  <summary class="dsl">

  ```gherkin
  <step> for each <element> located by <selector> "<expression>"
  ```
  <p>Executes a step for each element in a list of elements in the current scope with a default lookup timeout.</p>
  </summary>
  <p>Where</p>
  <ul>
    <li><code>&lt;step&gt;</code> is the DSL step or StepDef to execute for each element</li>
    <li><code>&lt;element&gt;</code> is the name that will be assigned to the current element occurrence (element is accessible in <code>&lt;step&gt;</code> by this name)</li>
    <li><code>&lt;selector&gt;</code> is the selector type, one of:
      <ul>
        <li><code>id</code> for selecting by id attribute</li>
        <li><code>name</code> for selecting by name attribute</li>
        <li><code>tag name</code> (or <code>tag</code> since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.46.0">v2.46.0</Link>) for selecting by tag name</li>
        <li><code>css selector</code> (or <code>css</code> since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.46.0">v2.46.0</Link>) for selecting by css expression</li>
        <li><code>class name</code> (or <code>class</code> since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.46.0">v2.46.0</Link>) for selecting by class attribute</li>
        <li><code>xpath</code> for selecting by XPath</li>
        <li><code>link text</code> for selecting by link text</li>
        <li><code>partial link text</code> for selecting by partial link text</li>
        <li><code>javascript</code> (or <code>js</code> since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.46.0">v2.46.0</Link>) for selecting <Link to="https://gweninterpreter.wordpress.com/2019/08/13/locating-web-elements-with-gwen/#byJavaScript">by JavaScript</Link></li>
      </ul>
    </li>
    <li><code>&lt;expression&gt;</code> is the selector expression returning one or many elements</li>
  </ul>
  <p>Example</p>

  ```gherkin {6}
    Given the todo field can be located by class "new-todo"
      And the active count can be located by css ".todo-count strong"
     When I navigate to "https://todomvc.com/examples/react"
      And I enter "Walk the dog" in the todo field
      And I enter "Feed the cat" in the todo field
      And I tick todo item for each todo item located by css ".todo-list .toggle"
     Then the active count should be "0"
  ```

  <div class="grid-3">
    <div><Link to="#step-for-each-element-located-by-selector-expression">Link</Link></div>
    <div align="center"></div>
    <div align="right">Since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.4.0">v2.4.0</Link></div>
  </div>
</details>

<details id="step-for-each-element-located-by-selector-expression-with-no-timeout">
  <summary class="dsl">

  ```gherkin
  <step> for each <element> located by <selector> "<expression>" with no <wait|timeout>
  ```
  <p>Executes a step for each element in a list of elements in the current scope with a no lookup timeout.</p>
  </summary>
  <p>Where</p>
  <ul>
    <li><code>&lt;step&gt;</code> is the DSL step or StepDef to execute for each element</li>
    <li><code>&lt;element&gt;</code> is the name that will be assigned to the current element occurrence (element is accessible in <code>&lt;step&gt;</code> by this name)</li>
    <li><code>&lt;selector&gt;</code> is the selector type, one of:
      <ul>
        <li><code>id</code> for selecting by id attribute</li>
        <li><code>name</code> for selecting by name attribute</li>
        <li><code>tag name</code> (or <code>tag</code> since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.46.0">v2.46.0</Link>) for selecting by tag name</li>
        <li><code>css selector</code> (or <code>css</code> since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.46.0">v2.46.0</Link>) for selecting by css expression</li>
        <li><code>class name</code> (or <code>class</code> since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.46.0">v2.46.0</Link>) for selecting by class attribute</li>
        <li><code>xpath</code> for selecting by XPath</li>
        <li><code>link text</code> for selecting by link text</li>
        <li><code>partial link text</code> for selecting by partial link text</li>
        <li><code>javascript</code> (or <code>js</code> since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.46.0">v2.46.0</Link>) for selecting <Link to="https://gweninterpreter.wordpress.com/2019/08/13/locating-web-elements-with-gwen/#byJavaScript">by JavaScript</Link></li>
      </ul>
    </li>
    <li><code>&lt;expression&gt;</code> is the selector expression returning one or many elements</li>
    <li><code>&lt;wait|timeout&gt;</code> is either <code>wait</code> or <code>timeout</code></li>
  </ul>
  <p>Example</p>

  ```gherkin {6}
    Given the todo field can be located by class "new-todo"
      And the active count can be located by css ".todo-count strong"
     When I navigate to "https://todomvc.com/examples/react"
      And I enter "Walk the dog" in the todo field
      And I enter "Feed the cat" in the todo field
      And I tick todo item for each todo item located by css ".todo-list .toggle" with no wait
     Then the active count should be "0"
  ```

  <div class="grid-3">
    <div><Link to="#step-for-each-element-located-by-selector-expression-with-no-timeout">Link</Link></div>
    <div align="center"></div>
    <div align="right">Since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.19.0">v2.19.0</Link></div>
  </div>
</details>

<details id="step-for-each-element-located-by-selector-expression-with-timeout">
  <summary class="dsl">

  ```gherkin
  <step> for each <element> located by <selector> "<expression>" with <timeoutSecs> second <wait|timeout>
  ```
  <p>Executes a step for each element in a list of elements in the current scope with a given lookup timeout.</p>
  </summary>
  <p>Where</p>
  <ul>
    <li><code>&lt;step&gt;</code> is the DSL step or StepDef to execute for each element</li>
    <li><code>&lt;element&gt;</code> is the name that will be assigned to the current element occurrence (element is accessible in <code>&lt;step&gt;</code> by this name)</li>
    <li><code>&lt;selector&gt;</code> is the selector type, one of:
      <ul>
        <li><code>id</code> for selecting by id attribute</li>
        <li><code>name</code> for selecting by name attribute</li>
        <li><code>tag name</code> (or <code>tag</code> since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.46.0">v2.46.0</Link>) for selecting by tag name</li>
        <li><code>css selector</code> (or <code>css</code> since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.46.0">v2.46.0</Link>) for selecting by css expression</li>
        <li><code>class name</code> (or <code>class</code> since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.46.0">v2.46.0</Link>) for selecting by class attribute</li>
        <li><code>xpath</code> for selecting by XPath</li>
        <li><code>link text</code> for selecting by link text</li>
        <li><code>partial link text</code> for selecting by partial link text</li>
        <li><code>javascript</code> (or <code>js</code> since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.46.0">v2.46.0</Link>) for selecting <Link to="https://gweninterpreter.wordpress.com/2019/08/13/locating-web-elements-with-gwen/#byJavaScript">by JavaScript</Link></li>
      </ul>
    </li>
    <li><code>&lt;expression&gt;</code> is the selector expression returning one or many elements</li>
    <li><code>&lt;timeoutSecs&gt;</code> is the timeout period in seconds</li>
    <li><code>&lt;wait|timeout&gt;</code> is either <code>wait</code> or <code>timeout</code></li>
  </ul>
  <p>Example</p>

  ```gherkin {6}
    Given the todo field can be located by class "new-todo"
      And the active count can be located by css ".todo-count strong"
     When I navigate to "https://todomvc.com/examples/react"
      And I enter "Walk the dog" in the todo field
      And I enter "Feed the cat" in the todo field
      And I tick todo item for each todo item located by css ".todo-list .toggle" with 5 second timeout
     Then the active count should be "0"
  ```

  <div class="grid-3">
    <div><Link to="#step-for-each-element-located-by-selector-expression-with-timeout">Link</Link></div>
    <div align="center"></div>
    <div align="right">Since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.19.0">v2.19.0</Link></div>
  </div>
</details>

<details id="step-for-each-element-located-by-selector-expression-in-container">
  <summary class="dsl">

  ```gherkin
  <step> for each <element> located by <selector> "<expression>" in <containerElement>
  ```
  <p>Executes a step for each element in a list of elements in a container element in the current scope with a default lookup timeout.</p>
  </summary>
  <p>Where</p>
  <ul>
    <li><code>&lt;step&gt;</code> is the DSL step or StepDef to execute for each element</li>
    <li><code>&lt;element&gt;</code> is the name that will be assigned to the current element occurrence (element is accessible in <code>&lt;step&gt;</code> by this name)</li>
    <li><code>&lt;selector&gt;</code> is the selector type, one of:
      <ul>
        <li><code>id</code> for selecting by id attribute</li>
        <li><code>name</code> for selecting by name attribute</li>
        <li><code>tag name</code> (or <code>tag</code> since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.46.0">v2.46.0</Link>) for selecting by tag name</li>
        <li><code>css selector</code> (or <code>css</code> since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.46.0">v2.46.0</Link>) for selecting by css expression</li>
        <li><code>class name</code> (or <code>class</code> since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.46.0">v2.46.0</Link>) for selecting by class attribute</li>
        <li><code>xpath</code> for selecting by XPath</li>
        <li><code>link text</code> for selecting by link text</li>
        <li><code>partial link text</code> for selecting by partial link text</li>
        <li><code>javascript</code> (or <code>js</code> since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.46.0">v2.46.0</Link>) for selecting <Link to="https://gweninterpreter.wordpress.com/2019/08/13/locating-web-elements-with-gwen/#byJavaScript">by JavaScript</Link></li>
      </ul>
    </li>
    <li><code>&lt;expression&gt;</code> is the selector expression returning one or many elements</li>
    <li><code>&lt;containerElement&gt;</code> is the name assigned to the container element</li>
  </ul>
  <p>Example</p>

  ```gherkin {7}
    Given the todo field can be located by class "new-todo"
      And the todo list can be located by class "todo-list"
      And the active count can be located by css ".todo-count strong"
     When I navigate to "https://todomvc.com/examples/react"
      And I enter "Walk the dog" in the todo field
      And I enter "Feed the cat" in the todo field
      And I tick todo item for each todo item located by class "toggle" in the todo list
     Then the active count should be "0"
  ```

  <div class="grid-3">
    <div><Link to="#step-for-each-element-located-by-selector-expression-in-container">Link</Link></div>
    <div align="center"></div>
    <div align="right">Since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.4.0">v2.4.0</Link></div>
  </div>
</details>

<details id="step-for-each-element-located-by-selector-expression-in-container-with-no-timeout">
  <summary class="dsl">

  ```gherkin
  <step> for each <element> located by <selector> "<expression>" in <containerElement> with no <wait|timeout>
  ```
  <p>Executes a step for each element in a list of elements in a container element in the current scope with no lookup timeout.</p>
  </summary>
  <p>Where</p>
  <ul>
    <li><code>&lt;step&gt;</code> is the DSL step or StepDef to execute for each element</li>
    <li><code>&lt;element&gt;</code> is the name that will be assigned to the current element occurrence (element is accessible in <code>&lt;step&gt;</code> by this name)</li>
    <li><code>&lt;selector&gt;</code> is the selector type, one of:
      <ul>
        <li><code>id</code> for selecting by id attribute</li>
        <li><code>name</code> for selecting by name attribute</li>
        <li><code>tag name</code> (or <code>tag</code> since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.46.0">v2.46.0</Link>) for selecting by tag name</li>
        <li><code>css selector</code> (or <code>css</code> since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.46.0">v2.46.0</Link>) for selecting by css expression</li>
        <li><code>class name</code> (or <code>class</code> since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.46.0">v2.46.0</Link>) for selecting by class attribute</li>
        <li><code>xpath</code> for selecting by XPath</li>
        <li><code>link text</code> for selecting by link text</li>
        <li><code>partial link text</code> for selecting by partial link text</li>
        <li><code>javascript</code> (or <code>js</code> since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.46.0">v2.46.0</Link>) for selecting <Link to="https://gweninterpreter.wordpress.com/2019/08/13/locating-web-elements-with-gwen/#byJavaScript">by JavaScript</Link></li>
      </ul>
    </li>
    <li><code>&lt;expression&gt;</code> is the selector expression returning one or many elements</li>
    <li><code>&lt;containerElement&gt;</code> is the name assigned to the container element</li>
    <li><code>&lt;wait|timeout&gt;</code> is either <code>wait</code> or <code>timeout</code></li>
  </ul>
  <p>Example</p>

  ```gherkin {7}
    Given the todo field can be located by class "new-todo"
      And the todo list can be located by class "todo-list"
      And the active count can be located by css ".todo-count strong"
     When I navigate to "https://todomvc.com/examples/react"
      And I enter "Walk the dog" in the todo field
      And I enter "Feed the cat" in the todo field
      And I tick todo item for each todo item located by class "toggle" in the todo list with no timeout
     Then the active count should be "0"
  ```

  <div class="grid-3">
    <div><Link to="#step-for-each-element-located-by-selector-expression-in-container-with-no-timeout">Link</Link></div>
    <div align="center"></div>
    <div align="right">Since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.19.0">v2.19.0</Link></div>
  </div>
</details>

<details id="step-for-each-element-located-by-selector-expression-in-container-with-timeout">
  <summary class="dsl">

  ```gherkin
  <step> for each <element> located by <selector> "<expression>" in <containerElement> with <timeoutSecs> second <wait|timeout>
  ```
  <p>Executes a step for each element in a list of elements in a container element in the current scope with a given lookup timeout.</p>
  </summary>
  <p>Where</p>
  <ul>
    <li><code>&lt;step&gt;</code> is the DSL step or StepDef to execute for each element</li>
    <li><code>&lt;element&gt;</code> is the name that will be assigned to the current element occurrence (element is accessible in <code>&lt;step&gt;</code> by this name)</li>
    <li><code>&lt;selector&gt;</code> is the selector type, one of:
      <ul>
        <li><code>id</code> for selecting by id attribute</li>
        <li><code>name</code> for selecting by name attribute</li>
        <li><code>tag name</code> (or <code>tag</code> since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.46.0">v2.46.0</Link>) for selecting by tag name</li>
        <li><code>css selector</code> (or <code>css</code> since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.46.0">v2.46.0</Link>) for selecting by css expression</li>
        <li><code>class name</code> (or <code>class</code> since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.46.0">v2.46.0</Link>) for selecting by class attribute</li>
        <li><code>xpath</code> for selecting by XPath</li>
        <li><code>link text</code> for selecting by link text</li>
        <li><code>partial link text</code> for selecting by partial link text</li>
        <li><code>javascript</code> (or <code>js</code> since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.46.0">v2.46.0</Link>) for selecting <Link to="https://gweninterpreter.wordpress.com/2019/08/13/locating-web-elements-with-gwen/#byJavaScript">by JavaScript</Link></li>
      </ul>
    </li>
    <li><code>&lt;expression&gt;</code> is the selector expression returning one or many elements</li>
    <li><code>&lt;containerElement&gt;</code> is the name assigned to the container element</li>
    <li><code>&lt;timeoutSecs&gt;</code> is the timeout period in seconds</li>
    <li><code>&lt;wait|timeout&gt;</code> is either <code>wait</code> or <code>timeout</code></li>
  </ul>
  <p>Example</p>

  ```gherkin {7}
    Given the todo field can be located by class "new-todo"
      And the todo list can be located by class "todo-list"
      And the active count can be located by css ".todo-count strong"
     When I navigate to "https://todomvc.com/examples/react"
      And I enter "Walk the dog" in the todo field
      And I enter "Feed the cat" in the todo field
      And I tick todo item for each todo item located by class "toggle" in the todo list with 3 second wait
     Then the active count should be "0"
  ```

  <div class="grid-3">
    <div><Link to="#step-for-each-element-located-by-selector-expression-in-container-with-timeout">Link</Link></div>
    <div align="center"></div>
    <div align="right">Since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.19.0">v2.19.0</Link></div>
  </div>
</details>

<details id="step-for-each-element-in-elements">
  <summary class="dsl">

  ```gherkin
  <step> for each <element> in <elements>
  ```
  <p>Executes a step for each element in a list of elements in the current scope with a default lookup timeout.</p>
  </summary>
  <p>Where</p>
  <ul>
    <li><code>&lt;step&gt;</code> is the DSL step or StepDef to execute for each element</li>
    <li><code>&lt;element&gt;</code> is the name that will be assigned to the current element occurrence (element is accessible in <code>&lt;step&gt;</code> by this name)</li>
    <li><code>&lt;elements&gt;</code> is the name of the locator binding returning multiple elements</li>
  </ul>
  <p>Example</p>

  ```gherkin {7}
    Given the todo field can be located by class "new-todo"
      And the active count can be located by css ".todo-count strong"
      And all todo items can be located by css ".todo-list .toggle"
     When I navigate to "https://todomvc.com/examples/react"
      And I enter "Walk the dog" in the todo field
      And I enter "Feed the cat" in the todo field
      And I tick todo item for each todo item in all todo items
     Then the active count should be "0"
  ```

  <div class="grid-3">
    <div><Link to="#step-for-each-element-in-elements">Link</Link></div>
    <div align="center"></div>
    <div align="right">Since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.4.0">v2.4.0</Link></div>
  </div>
</details>

<details id="step-for-each-data-record">
  <summary class="dsl">

  ```gherkin
  <step> for each data record
  ```
  <p>Executes a step for each record in a data table.</p>
  </summary>
  <p>Where</p>
  <ul>
    <li><code>&lt;step&gt;</code> is the DSL step or StepDef to execute for each record</li>
    <li>
      The following implicit bindings are available in <code>&lt;step&gt;</code>:
      <ul>
        <li><code>record.number</code> is the current record number (starting at 1)</li>
        <li><code>record.index</code> is the record index (starting at 0), since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.30.1">v2.30.1</Link></li>
        <li><code>data[name]</code> is the data of an element in the current record, where <code>name</code> is the name of a record element</li>
      </ul>
    </li>
  </ul>
  <p>Example</p>

  ```gherkin {7}
    @StepDef
    @DataTable(horizontal="Item")
    Scenario: I have the following active items
        Given the todo field can be located by class "new-todo"
          And the active count can be located by css ".todo-count strong"
         When I navigate to "https://todomvc.com/examples/react"
          And I enter "${data[Item]}" in the todo field for each data record
         Then the active count should be "3"

    Scenario: my scenario
        Given I have the following active items
              | Walk the dog |
              | Get the milk |
              | Feed the cat |
  ```

  <div class="grid-3">
    <div><Link to="#step-for-each-data-record">Link</Link></div>
    <div align="center"></div>
    <div align="right">Since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.8.0">v2.8.0</Link></div>
  </div>
</details>

<details id="for-each-entry-in-source-delimited-by">
  <summary class="dsl">

  ```gherkin
  <step> for each <entry> in <source> delimited by "<delimiter>"
  ```
  <p></p>
  </summary>
  <p>Where</p>
  <ul>
    <li><code>&lt;step&gt;</code> is the DSL step or StepDef to execute for each delimited entry</li>
    <li><code>&lt;entry&gt;</code> is the name that will be assigned to the current entry occurrence (entry is accessible in <code>&lt;step&gt;</code> by this name)</li>
    <li><code>&lt;source&gt;</code> is the name of the binding containing the delimitted values
      <ul>
        <li>Can be the name of any binding that contains or resolves to a delmited list of values, including a web element in which case the text in the element will be dynamically retrieved and used.</li>
      </ul>
    </li>
    <li><code>&lt;delimiter&gt;</code> the delimiter used in <code>&lt;source&gt;</code> to separate each value</li>
  </ul>
  <p>Example</p>

  ```gherkin {5}
    Given the todo field can be located by class "new-todo"
      And the active count can be located by css ".todo-count strong"
      And my items is "Walk the dog, Feed the cat, Get the milk"
     When I navigate to "https://todomvc.com/examples/react"
      And I enter todo item in the todo field for each todo item in my items delimited by ","
     Then the active count should be "3"
  ```

  <div class="grid-3">
    <div><Link to="#for-each-entry-in-source-delimited-by">Link</Link></div>
    <div align="center"></div>
    <div align="right">Since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v2.13.0">v2.13.0</Link></div>
  </div>
</details>

### Until

<details id="step-until-condition">
  <summary class="dsl">

  ```gherkin
  <step> until <condition>
  ```
  <p>Repeatedly exectues a step until a condition is satisfied using a default delay and timeout period. The default delay between iterations is one tenth of the configured <code>gwen.web.wait.seconds</code> setting (or 1 second if not set). The default timeout period is 30 times the delay.</p>
  </summary>
  <p>Where</p>
  <ul>
    <li><code>&lt;step&gt;</code> is the DSL step or StepDef to execute in each iteration</li>
    <li><code>&lt;condition&gt;</code> the name of a javascript binding containing a function that will return true when the condition is satisfied and false when it is not.</li>
    <li>
      The following implicit binding is available in <code>&lt;step&gt;</code>:
      <ul>
        <li><code>iteration number</code> is the current iteration number (starting at 1)</li>
      </ul>
    </li>
  </ul>
  <p>Example</p>

  ```gherkin {8}
    @StepDef
    Scenario: I increment the counter
        Given counter is defined by js "${counter} + 1"
          And counter > 3 is defined by javascript "${counter} > 3"
    
    Scenario: repeat until
        Given counter is defined by js "0"
         When I increment the counter until counter > 3
         Then counter should be "4"
  ```

  <div class="grid-3">
    <div><Link to="#step-until-condition">Link</Link></div>
    <div align="center"></div>
    <div align="right">Since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v1.0.0">v1.0.0</Link></div>
  </div>
</details>

### While

<details id="step-while-condition">
  <summary class="dsl">

  ```gherkin
  <step> while <condition>
  ```
  <p>Repeatedly exectues a step while a condition is satisfied using a default delay and timeout period. The default delay between iterations is one tenth of the configured <code>gwen.web.wait.seconds</code> setting (or 1 second if not set). The default timeout period is 30 times the delay.</p>
  </summary>
  <p>Where</p>
  <ul>
    <li><code>&lt;step&gt;</code> is the DSL step or StepDef to execute in each iteration</li>
    <li><code>&lt;condition&gt;</code> the name of a javascript binding containing a function that will return true when the condition is satisfied and false when it is not.</li>
    <li>
      The following implicit binding is available in <code>&lt;step&gt;</code>:
      <ul>
        <li><code>iteration number</code> is the current iteration number (starting at 1)</li>
      </ul>
    </li>
  </ul>
  <p>Example</p>

  ```gherkin {9}
    @StepDef
    Scenario: I increment the counter
        Given counter is defined by js "${counter} + 1"
          And counter < 4 is defined by js "${counter} < 4"
    
    Scenario: repeat while
        Given counter is defined by js "-1"
         When I increment the counter
          And I increment the counter while counter < 4
         Then counter should be "4"
  ```

  <div class="grid-3">
    <div><Link to="#step-while-condition">Link</Link></div>
    <div align="center"></div>
    <div align="right">Since <Link to="https://github.com/gwen-interpreter/gwen-web/releases/tag/v1.0.0">v1.0.0</Link></div>
  </div>
</details>
