
import Link from '@docusaurus/Link';
import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

# JUnit-XML Reports

Gwen can generete JUnit-XML reports enabling you to integrate evaluation results with build servers (such as [Jenkins](https://www.jenkins.io/)) and other tools.

## Generate JUnit-XML

<Tabs
  groupId="setup"
  defaultValue="standalone"
  values={[
    {label: 'Standalone', value: 'standalone'},
    {label: 'Workspace', value: 'workspace'},
    {label: 'JS Project', value: 'js'},
  ]}>

<TabItem value="standalone">

<Tabs
  groupId="os"
  defaultValue="linux"
  values={[
    {label: 'Linux', value: 'linux'},
    {label: 'Windows', value: 'windows'},
  ]}>

<TabItem value="linux">

To generate JUnit-XML reports only

```shell
./gwen -b -r output/reports -f junit features/todo
```

To generate HTML and JUnit-XML reports

```shell
./gwen -b -r output/reports -f html,junit features/todo
```

</TabItem>

<TabItem value="windows">

To generate JUnit-XML reports only

```shell
gwen -b -r output\reports -f junit features\todo
```

To generate HTML and JUnit-XML reports

```shell
gwen -b -r output\reports -f html,junit features\todo
```

</TabItem>

</Tabs>

<p>Output</p>

- The JUnit-XML report files will be generated at <code>output/reports/junit/TEST-*.xml</code>. You can set this path in your Jenkins reporting configuration for example.
- The HTML report will be generated at <code>output/reports/index.html</code>.

</TabItem>

<TabItem value="workspace">

:::caution Deprecated

[Gwen workspaces](https://gweninterpreter.wordpress.com/2017/12/18/gwen-workspaces/) are deprecated in favor of JS projects coming to Gwen 3.

:::

<Tabs
  groupId="os"
  defaultValue="linux"
  values={[
    {label: 'Linux', value: 'linux'},
    {label: 'Windows', value: 'windows'},
  ]}>

<TabItem value="linux">

To generate JUnit-XML reports only

```shell
./gwen -b -f junit features/todo
```

To generate HTML and JUnit-XML reports

```shell
./gwen -b -f html,junit features/todo
```

</TabItem>

<TabItem value="windows">

To generate JUnit-XML reports only

```shell
gwen -b -f junit features\todo
```

To generate HTML and JUnit-XML reports

```shell
gwen -b -f html,junit features\todo
```

</TabItem>

</Tabs>

<p>Output</p>

- The JUnit-XML report files will be generated at <code>target/reports/junit/TEST-*.xml</code>. You can set this path in your Jenkins reporting configuration for example.
- The HTML report will be generated at <code>target/reports/index.html</code>.

</TabItem>

<TabItem value="js">

:::info Coming to Gwen 3

Support for JS projects coming soon to Gwen 3.

:::

</TabItem>

</Tabs>

## Sample output

*File: target/reports/junit/TEST-features-todo-Todo.feature.xml*
```xml
<?xml version="1.0" encoding="UTF-8"?>
<testsuite hostname="machine-hostname" 
           name="features/todo/Todo.feature.Feature: Add todo items" 
           package="features/todo/Todo.feature" 
           tests="1" 
           errors="0" 
           failures="0" 
           skipped="0" 
           time="8.320" 
           timestamp="2021-10-09T01:48:54.888Z">
  <properties>
    ..
  </properties>
  <testcase name="Scenario 0001: Create todo list" 
            time="8.153601684" 
            status="Passed"/>
</testsuite>
```